// Material alpha identities shared by every mesh shader stage. The CPU-side
// contract lives in `tecs.internal.render.meshconstants`.
const int MESH_ALPHA_OPAQUE = 0;
const int MESH_ALPHA_MASK = 1;
const int MESH_ALPHA_BLEND = 2;
