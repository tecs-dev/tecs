-- The suite lives at `tecs.framepackettest` because it imports `tecs.internal`
-- modules, which the checker restricts to importers whose first namespace
-- segment is `tecs`. Discovery only reads `tests/*test.nupp`, so this forwards.
return require("tecs.framepackettest")
