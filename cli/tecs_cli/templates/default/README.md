# Tecs Game

Created with `tecs new`.

## Commands

```sh
tecs check
tecs build
tecs run
```

The hello demo enables the MCP server and runtime debugger by default. Press
`Ctrl+/` in the running game to toggle the debugger.
