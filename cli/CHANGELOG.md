# Changelog

All notable changes to this project will be documented in this file.

When cutting a release, bump the version in `tecs_cli/cli.lua` (`VERSION`) and
the rockspec filename/`version` together.

## [Unreleased]

### Added

- Initial release.
