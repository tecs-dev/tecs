---
title: tecs.math
description: Allocation-free two-dimensional vector and angle math.
outline: [2, 3]
---

# tecs.math

`tecs.math` operates on separate numeric coordinates and returns vector results
as two values:

```lua
local directionX, directionY = tecs.math.normalize(targetX - x, targetY - y)
local nextX, nextY = tecs.math.moveTowards(x, y, targetX, targetY, speed * dt)
```

There is no vector object to allocate or convert. The caller decides whether
the result goes into locals, an ordinary component, or an FFI component.
Squared length and distance functions avoid a square root when only a
comparison is needed.

Every angle is in radians. A positive quarter turn maps `(1, 0)` to `(0, 1)`.
That appears clockwise in the usual screen coordinate system, where y grows
downward. `angleBetween` discards direction, while `signedAngleBetween` uses
the same sign convention as `rotate`.

Operations with no unique geometric answer use stable values:

| Operation           | Degenerate input       | Result              |
| ------------------- | ---------------------- | ------------------- |
| `normalize`         | zero vector            | `(0, 0)`            |
| `angle`             | zero vector            | `0`                 |
| angle-between forms | either vector is zero  | `0`                 |
| `project`           | zero projection axis   | `(0, 0)`            |
| `reflect`           | zero reflecting normal | the original vector |

`project` and `reflect` accept axes and normals of any nonzero length.
`lerp` is unclamped and may extrapolate. `moveTowards` never overshoots and a
nonpositive distance leaves the starting point unchanged.
<!-- @generated by docs/scripts/reference.py from src/tecs/math.tl. Do not edit below this line. -->

## Reference

Every function and type this module carries, rendered from `src/tecs/math.tl`.

<a id="tecs.math.add"></a>

### tecs.math.add

<pre><code v-pre>function <a href="#tecs.math.add">tecs.math.add</a>(ax: number, ay: number, bx: number, by: number): number, number
</code></pre>

Adds two vectors.

#### Parameters

| Type                      | Name                  | Description                   |
| ------------------------- | --------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First vector's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First vector's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second vector's y coordinate. |

#### Returns

| Type                      | Description                    |
| ------------------------- | ------------------------------ |
| <code v-pre>number</code> | The sum's x and y coordinates. |
| <code v-pre>number</code> |                                |

<a id="tecs.math.angle"></a>

### tecs.math.angle

<pre><code v-pre>function <a href="#tecs.math.angle">tecs.math.angle</a>(x: number, y: number): number
</code></pre>

Computes a vector's direction.

#### Parameters

| Type                      | Name                 | Description            |
| ------------------------- | -------------------- | ---------------------- |
| <code v-pre>number</code> | <code v-pre>x</code> | Vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code> | Vector's y coordinate. |

#### Returns

| Type                      | Description                                               |
| ------------------------- | --------------------------------------------------------- |
| <code v-pre>number</code> | `atan2(y, x)` in `[-pi, pi]`. A zero vector returns zero. |

<a id="tecs.math.angleBetween"></a>

### tecs.math.angleBetween

<pre><code v-pre>function <a href="#tecs.math.angleBetween">tecs.math.angleBetween</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes the smaller unsigned angle between two vectors.

#### Parameters

| Type                      | Name                  | Description                   |
| ------------------------- | --------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First vector's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First vector's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second vector's y coordinate. |

#### Returns

| Type                      | Description                                                   |
| ------------------------- | ------------------------------------------------------------- |
| <code v-pre>number</code> | Radians in `[0, pi]`. If either vector is zero, returns zero. |

<a id="tecs.math.cross"></a>

### tecs.math.cross

<pre><code v-pre>function <a href="#tecs.math.cross">tecs.math.cross</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes the scalar two-dimensional cross product.

#### Parameters

| Type                      | Name                  | Description                   |
| ------------------------- | --------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First vector's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First vector's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second vector's y coordinate. |

#### Returns

| Type                      | Description                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------ |
| <code v-pre>number</code> | `ax * by - ay * bx`; positive puts the second vector on the positive rotation side of the first. |

<a id="tecs.math.deltaAngle"></a>

### tecs.math.deltaAngle

<pre><code v-pre>function <a href="#tecs.math.deltaAngle">tecs.math.deltaAngle</a>(from: number, to: number): number
</code></pre>

Computes the shortest signed turn from one angle to another.

#### Parameters

| Type                      | Name                    | Description                   |
| ------------------------- | ----------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>from</code> | Starting angle in radians.    |
| <code v-pre>number</code> | <code v-pre>to</code>   | Destination angle in radians. |

#### Returns

| Type                      | Description                                                            |
| ------------------------- | ---------------------------------------------------------------------- |
| <code v-pre>number</code> | `to - from` wrapped to `[-pi, pi)`. An exact half-turn is negative pi. |

<a id="tecs.math.distance"></a>

### tecs.math.distance

<pre><code v-pre>function <a href="#tecs.math.distance">tecs.math.distance</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes the distance between two points.

#### Parameters

| Type                      | Name                  | Description                  |
| ------------------------- | --------------------- | ---------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First point's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First point's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second point's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second point's y coordinate. |

#### Returns

| Type                      | Description                                            |
| ------------------------- | ------------------------------------------------------ |
| <code v-pre>number</code> | The nonnegative Euclidean distance between the points. |

<a id="tecs.math.distanceSquared"></a>

### tecs.math.distanceSquared

<pre><code v-pre>function <a href="#tecs.math.distanceSquared">tecs.math.distanceSquared</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes squared distance without taking a square root.

Prefer this to `distance` for radius tests.

#### Parameters

| Type                      | Name                  | Description                  |
| ------------------------- | --------------------- | ---------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First point's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First point's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second point's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second point's y coordinate. |

#### Returns

| Type                      | Description                                        |
| ------------------------- | -------------------------------------------------- |
| <code v-pre>number</code> | The squared Euclidean distance between the points. |

<a id="tecs.math.dot"></a>

### tecs.math.dot

<pre><code v-pre>function <a href="#tecs.math.dot">tecs.math.dot</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes the dot product of two vectors.

#### Parameters

| Type                      | Name                  | Description                   |
| ------------------------- | --------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First vector's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First vector's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second vector's y coordinate. |

#### Returns

| Type                      | Description          |
| ------------------------- | -------------------- |
| <code v-pre>number</code> | `ax * bx + ay * by`. |

<a id="tecs.math.length"></a>

### tecs.math.length

<pre><code v-pre>function <a href="#tecs.math.length">tecs.math.length</a>(x: number, y: number): number
</code></pre>

Computes vector length.

#### Parameters

| Type                      | Name                 | Description            |
| ------------------------- | -------------------- | ---------------------- |
| <code v-pre>number</code> | <code v-pre>x</code> | Vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code> | Vector's y coordinate. |

#### Returns

| Type                      | Description                       |
| ------------------------- | --------------------------------- |
| <code v-pre>number</code> | The nonnegative Euclidean length. |

<a id="tecs.math.lengthSquared"></a>

### tecs.math.lengthSquared

<pre><code v-pre>function <a href="#tecs.math.lengthSquared">tecs.math.lengthSquared</a>(x: number, y: number): number
</code></pre>

Computes squared vector length without taking a square root.

Prefer this to `length` when comparing magnitudes.

#### Parameters

| Type                      | Name                 | Description            |
| ------------------------- | -------------------- | ---------------------- |
| <code v-pre>number</code> | <code v-pre>x</code> | Vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code> | Vector's y coordinate. |

#### Returns

| Type                      | Description      |
| ------------------------- | ---------------- |
| <code v-pre>number</code> | `x * x + y * y`. |

<a id="tecs.math.lerp"></a>

### tecs.math.lerp

<pre><code v-pre>function <a href="#tecs.math.lerp">tecs.math.lerp</a>(ax: number, ay: number, bx: number, by: number, t: number): number, number
</code></pre>

Interpolates linearly between two points.

#### Parameters

| Type                      | Name                  | Description                                                                                                                        |
| ------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | Starting point's x coordinate.                                                                                                     |
| <code v-pre>number</code> | <code v-pre>ay</code> | Starting point's y coordinate.                                                                                                     |
| <code v-pre>number</code> | <code v-pre>bx</code> | Destination point's x coordinate.                                                                                                  |
| <code v-pre>number</code> | <code v-pre>by</code> | Destination point's y coordinate.                                                                                                  |
| <code v-pre>number</code> | <code v-pre>t</code>  | Unclamped interpolation amount. Zero returns the start, one returns the destination, and values outside that interval extrapolate. |

#### Returns

| Type                      | Description                           |
| ------------------------- | ------------------------------------- |
| <code v-pre>number</code> | The interpolated x and y coordinates. |
| <code v-pre>number</code> |                                       |

<a id="tecs.math.moveTowards"></a>

### tecs.math.moveTowards

<pre><code v-pre>function <a href="#tecs.math.moveTowards">tecs.math.moveTowards</a>(x: number, y: number, targetX: number, targetY: number, maxDistance: number): number, number
</code></pre>

Moves a point toward another by at most a given distance.

#### Parameters

| Type                      | Name                           | Description                                                                           |
| ------------------------- | ------------------------------ | ------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | <code v-pre>x</code>           | Starting point's x coordinate.                                                        |
| <code v-pre>number</code> | <code v-pre>y</code>           | Starting point's y coordinate.                                                        |
| <code v-pre>number</code> | <code v-pre>targetX</code>     | Destination point's x coordinate.                                                     |
| <code v-pre>number</code> | <code v-pre>targetY</code>     | Destination point's y coordinate.                                                     |
| <code v-pre>number</code> | <code v-pre>maxDistance</code> | Greatest distance to travel. A nonpositive value leaves the starting point unchanged. |

#### Returns

| Type                      | Description                                                                                      |
| ------------------------- | ------------------------------------------------------------------------------------------------ |
| <code v-pre>number</code> | The moved x and y coordinates. Reaching the destination returns it exactly and never overshoots. |
| <code v-pre>number</code> |                                                                                                  |

<a id="tecs.math.normalize"></a>

### tecs.math.normalize

<pre><code v-pre>function <a href="#tecs.math.normalize">tecs.math.normalize</a>(x: number, y: number): number, number
</code></pre>

Produces a unit vector in the same direction.

#### Parameters

| Type                      | Name                 | Description            |
| ------------------------- | -------------------- | ---------------------- |
| <code v-pre>number</code> | <code v-pre>x</code> | Vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code> | Vector's y coordinate. |

#### Returns

| Type                      | Description                                                                                     |
| ------------------------- | ----------------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | The normalized x and y coordinates. A zero vector returns `(0, 0)` because it has no direction. |
| <code v-pre>number</code> |                                                                                                 |

<a id="tecs.math.project"></a>

### tecs.math.project

<pre><code v-pre>function <a href="#tecs.math.project">tecs.math.project</a>(x: number, y: number, ontoX: number, ontoY: number): number, number
</code></pre>

Projects a vector onto another vector.

#### Parameters

| Type                      | Name                     | Description                            |
| ------------------------- | ------------------------ | -------------------------------------- |
| <code v-pre>number</code> | <code v-pre>x</code>     | Vector being projected's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code>     | Vector being projected's y coordinate. |
| <code v-pre>number</code> | <code v-pre>ontoX</code> | Projection axis's x coordinate.        |
| <code v-pre>number</code> | <code v-pre>ontoY</code> | Projection axis's y coordinate.        |

#### Returns

| Type                      | Description                                                                                       |
| ------------------------- | ------------------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | The projected x and y coordinates. The axis need not be normalized; a zero axis returns `(0, 0)`. |
| <code v-pre>number</code> |                                                                                                   |

<a id="tecs.math.reflect"></a>

### tecs.math.reflect

<pre><code v-pre>function <a href="#tecs.math.reflect">tecs.math.reflect</a>(x: number, y: number, normalX: number, normalY: number): number, number
</code></pre>

Reflects a vector across the line perpendicular to a normal.

#### Parameters

| Type                      | Name                       | Description                            |
| ------------------------- | -------------------------- | -------------------------------------- |
| <code v-pre>number</code> | <code v-pre>x</code>       | Vector being reflected's x coordinate. |
| <code v-pre>number</code> | <code v-pre>y</code>       | Vector being reflected's y coordinate. |
| <code v-pre>number</code> | <code v-pre>normalX</code> | Surface normal's x coordinate.         |
| <code v-pre>number</code> | <code v-pre>normalY</code> | Surface normal's y coordinate.         |

#### Returns

| Type                      | Description                                                                                                      |
| ------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | The reflected x and y coordinates. The normal need not be normalized; a zero normal returns the original vector. |
| <code v-pre>number</code> |                                                                                                                  |

<a id="tecs.math.rotate"></a>

### tecs.math.rotate

<pre><code v-pre>function <a href="#tecs.math.rotate">tecs.math.rotate</a>(x: number, y: number, radians: number): number, number
</code></pre>

Rotates a vector around the origin.

#### Parameters

| Type                      | Name                       | Description                                                             |
| ------------------------- | -------------------------- | ----------------------------------------------------------------------- |
| <code v-pre>number</code> | <code v-pre>x</code>       | Vector's x coordinate.                                                  |
| <code v-pre>number</code> | <code v-pre>y</code>       | Vector's y coordinate.                                                  |
| <code v-pre>number</code> | <code v-pre>radians</code> | Rotation in radians. A positive quarter turn maps `(1, 0)` to `(0, 1)`. |

#### Returns

| Type                      | Description                      |
| ------------------------- | -------------------------------- |
| <code v-pre>number</code> | The rotated x and y coordinates. |
| <code v-pre>number</code> |                                  |

<a id="tecs.math.scale"></a>

### tecs.math.scale

<pre><code v-pre>function <a href="#tecs.math.scale">tecs.math.scale</a>(x: number, y: number, factor: number): number, number
</code></pre>

Multiplies both coordinates by a scalar.

#### Parameters

| Type                      | Name                      | Description                             |
| ------------------------- | ------------------------- | --------------------------------------- |
| <code v-pre>number</code> | <code v-pre>x</code>      | Vector's x coordinate.                  |
| <code v-pre>number</code> | <code v-pre>y</code>      | Vector's y coordinate.                  |
| <code v-pre>number</code> | <code v-pre>factor</code> | Multiplier applied to both coordinates. |

#### Returns

| Type                      | Description                     |
| ------------------------- | ------------------------------- |
| <code v-pre>number</code> | The scaled x and y coordinates. |
| <code v-pre>number</code> |                                 |

<a id="tecs.math.signedAngleBetween"></a>

### tecs.math.signedAngleBetween

<pre><code v-pre>function <a href="#tecs.math.signedAngleBetween">tecs.math.signedAngleBetween</a>(ax: number, ay: number, bx: number, by: number): number
</code></pre>

Computes the smaller signed angle from one vector to another.

#### Parameters

| Type                      | Name                  | Description                        |
| ------------------------- | --------------------- | ---------------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | Starting vector's x coordinate.    |
| <code v-pre>number</code> | <code v-pre>ay</code> | Starting vector's y coordinate.    |
| <code v-pre>number</code> | <code v-pre>bx</code> | Destination vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Destination vector's y coordinate. |

#### Returns

| Type                      | Description                                                                                             |
| ------------------------- | ------------------------------------------------------------------------------------------------------- |
| <code v-pre>number</code> | Radians in `[-pi, pi]`, positive in the same sense as `rotate`. If either vector is zero, returns zero. |

<a id="tecs.math.subtract"></a>

### tecs.math.subtract

<pre><code v-pre>function <a href="#tecs.math.subtract">tecs.math.subtract</a>(ax: number, ay: number, bx: number, by: number): number, number
</code></pre>

Subtracts the second vector from the first.

#### Parameters

| Type                      | Name                  | Description                   |
| ------------------------- | --------------------- | ----------------------------- |
| <code v-pre>number</code> | <code v-pre>ax</code> | First vector's x coordinate.  |
| <code v-pre>number</code> | <code v-pre>ay</code> | First vector's y coordinate.  |
| <code v-pre>number</code> | <code v-pre>bx</code> | Second vector's x coordinate. |
| <code v-pre>number</code> | <code v-pre>by</code> | Second vector's y coordinate. |

#### Returns

| Type                      | Description                           |
| ------------------------- | ------------------------------------- |
| <code v-pre>number</code> | The difference's x and y coordinates. |
| <code v-pre>number</code> |                                       |

<a id="tecs.math.wrapAngle"></a>

### tecs.math.wrapAngle

<pre><code v-pre>function <a href="#tecs.math.wrapAngle">tecs.math.wrapAngle</a>(radians: number): number
</code></pre>

Wraps an angle to one turn centered on zero.

#### Parameters

| Type                      | Name                       | Description                |
| ------------------------- | -------------------------- | -------------------------- |
| <code v-pre>number</code> | <code v-pre>radians</code> | A finite angle in radians. |

#### Returns

| Type                      | Description                                                              |
| ------------------------- | ------------------------------------------------------------------------ |
| <code v-pre>number</code> | The equivalent angle in `[-pi, pi)`, so positive pi becomes negative pi. |
