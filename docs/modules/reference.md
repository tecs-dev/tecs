---
# @generated by docs/scripts/reference.sh. Do not edit.
description: "Generated signatures for every value and type the tecs module exports, rendered from src/tecs/init.tl"
outline: [2, 2]
---

# Generated reference

Every value and type `tecs` carries, rendered from `src/tecs/init.tl` and `src/tecs/ecs.tl` by
[tealdoc](https://github.com/teal-language/tealdoc). Signatures, doc comments, parameters and returns, checked
against a fresh render so it cannot drift from the code. The modules come first, alphabetically, and everything
under `tecs.ecs` follows.

This is a reference, not an explanation. The pages in the [module index](/modules/) say why each module is shaped
the way it is, which is the part a generator does not know; read those first and come here for the exact spelling.

The `tecs.` prefix below is what a game writes. Every module but `tecs.ecs` resolves on first access, so reading
one loads it and requiring `tecs` does not.


<a id="tecs.Archetype"></a>
## tecs.Archetype
```teal
type tecs.Archetype = types.Archetype
```
<a id="tecs.ArchetypeEntityObserver"></a>
## tecs.ArchetypeEntityObserver
```teal
type tecs.ArchetypeEntityObserver = types.ArchetypeEntityObserver
```
<a id="tecs.Bundle"></a>
## tecs.Bundle
```teal
type tecs.Bundle = types.Bundle
```
<a id="tecs.BundleDef"></a>
## tecs.BundleDef
```teal
type tecs.BundleDef = types.Bundle.Definition
```
<a id="tecs.Component"></a>
## tecs.Component
```teal
type tecs.Component = types.components.Component
```
<a id="tecs.ComponentOptions"></a>
## tecs.ComponentOptions
```teal
type tecs.ComponentOptions = generic<C> types.components.ComponentOptions<C>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> |  |  |

<a id="tecs.Context"></a>
## tecs.Context
```teal
type tecs.Context = types.Context
```
<a id="tecs.DoubleArray"></a>
## tecs.DoubleArray
```teal
type tecs.DoubleArray = types.DoubleArray
```
<a id="tecs.Event"></a>
## tecs.Event
```teal
type tecs.Event = types.events.Event
```
<a id="tecs.EventInit"></a>
## tecs.EventInit
```teal
type tecs.EventInit = types.events.EventInit
```
<a id="tecs.EventListener"></a>
## tecs.EventListener
```teal
type tecs.EventListener = types.events.EventListener
```
<a id="tecs.FFIComponentOptions"></a>
## tecs.FFIComponentOptions
```teal
type tecs.FFIComponentOptions = generic<C> types.components.FFIComponentOptions<C>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> |  |  |

<a id="tecs.FFIRelationshipOptions"></a>
## tecs.FFIRelationshipOptions
```teal
type tecs.FFIRelationshipOptions = generic<R is Relationship> types.components.FFIRelationshipOptions<R>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.Relationship">Relationship</a></code> |  |

<a id="tecs.FixedOverload"></a>
## tecs.FixedOverload
```teal
type tecs.FixedOverload = types.FixedOverload
```
<a id="tecs.Key"></a>
## tecs.Key
```teal
type tecs.Key = generic<T> types.Context.Key<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.MessageBus"></a>
## tecs.MessageBus
```teal
type tecs.MessageBus = types.events.MessageBus
```
<a id="tecs.Phase"></a>
## tecs.Phase
```teal
type tecs.Phase = types.Phase
```
<a id="tecs.Pipeline"></a>
## tecs.Pipeline
```teal
type tecs.Pipeline = types.Pipeline
```
<a id="tecs.Plugin"></a>
## tecs.Plugin
```teal
type tecs.Plugin = types.Plugin
```
<a id="tecs.Query"></a>
## tecs.Query
```teal
type tecs.Query = types.Query
```
<a id="tecs.QueryCursor"></a>
## tecs.QueryCursor
```teal
type tecs.QueryCursor = types.Query.Cursor
```
<a id="tecs.QueryDescriptor"></a>
## tecs.QueryDescriptor
```teal
type tecs.QueryDescriptor = types.Query.Descriptor
```
<a id="tecs.Relationship"></a>
## tecs.Relationship
```teal
type tecs.Relationship = types.components.Relationship
```
<a id="tecs.RelationshipOptions"></a>
## tecs.RelationshipOptions
```teal
type tecs.RelationshipOptions = generic<R is Relationship> types.components.RelationshipOptions<R>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.Relationship">Relationship</a></code> |  |

<a id="tecs.ScalarComponent"></a>
## tecs.ScalarComponent
```teal
type tecs.ScalarComponent = generic<T> types.components.ScalarComponent<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.ScalarComponentOptions"></a>
## tecs.ScalarComponentOptions
```teal
type tecs.ScalarComponentOptions = generic<T> types.components.ScalarComponentOptions<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.Snapshot"></a>
## tecs.Snapshot
```teal
type tecs.Snapshot = types.World.Snapshot
```
<a id="tecs.SnapshotComponentTableEntry"></a>
## tecs.SnapshotComponentTableEntry
```teal
type tecs.SnapshotComponentTableEntry = types.World.SnapshotComponentTableEntry
```
<a id="tecs.SnapshotHandler"></a>
## tecs.SnapshotHandler
```teal
type tecs.SnapshotHandler = types.World.SnapshotHandler
```
<a id="tecs.SnapshotOptions"></a>
## tecs.SnapshotOptions
```teal
type tecs.SnapshotOptions = types.World.SnapshotOptions
```
<a id="tecs.SnapshotOutput"></a>
## tecs.SnapshotOutput
```teal
type tecs.SnapshotOutput = types.World.SnapshotOutput
```
<a id="tecs.SnapshotPrelude"></a>
## tecs.SnapshotPrelude
```teal
type tecs.SnapshotPrelude = types.World.SnapshotPrelude
```
<a id="tecs.StatePolicy"></a>
## tecs.StatePolicy
```teal
type tecs.StatePolicy = types.StatePolicy
```
<a id="tecs.Stats"></a>
## tecs.Stats
```teal
type tecs.Stats = types.World.Stats
```
<a id="tecs.System"></a>
## tecs.System
```teal
type tecs.System = types.System
```
<a id="tecs.SystemConfig"></a>
## tecs.SystemConfig
```teal
type tecs.SystemConfig = types.SystemConfig
```
<a id="tecs.TagComponentOptions"></a>
## tecs.TagComponentOptions
```teal
type tecs.TagComponentOptions = types.components.TagComponentOptions
```
<a id="tecs.World"></a>
## tecs.World
```teal
type tecs.World = types.World
```
<a id="tecs.animation"></a>
## tecs.animation
```teal
tecs.animation: animation
```
<a id="tecs.application"></a>
## tecs.application
```teal
tecs.application: application
```
<a id="tecs.assets"></a>
## tecs.assets
```teal
tecs.assets: assets
```
<a id="tecs.audio"></a>
## tecs.audio
```teal
tecs.audio: audio
```
<a id="tecs.camera"></a>
## tecs.camera
```teal
tecs.camera: camera
```
<a id="tecs.capabilities"></a>
## tecs.capabilities
```teal
tecs.capabilities: capabilities
```
<a id="tecs.clipboard"></a>
## tecs.clipboard
```teal
tecs.clipboard: clipboard
```
<a id="tecs.clock"></a>
## tecs.clock
```teal
tecs.clock: clock
```
<a id="tecs.components"></a>
## tecs.components
```teal
tecs.components: components
```
<a id="tecs.compress"></a>
## tecs.compress
```teal
tecs.compress: compress
```
<a id="tecs.ecs"></a>
## tecs.ecs
```teal
tecs.ecs: ecs
```

Worlds, components, queries, systems, events and resources. The one
half that reaches nothing below Lua, so it is here rather than
resolved on demand.
<a id="tecs.events"></a>
## tecs.events
```teal
tecs.events: events
```
<a id="tecs.filesystem"></a>
## tecs.filesystem
```teal
tecs.filesystem: filesystem
```
<a id="tecs.future"></a>
## tecs.future
```teal
tecs.future: future
```
<a id="tecs.gamepad"></a>
## tecs.gamepad
```teal
tecs.gamepad: gamepad
```
<a id="tecs.hash"></a>
## tecs.hash
```teal
tecs.hash: hash
```
<a id="tecs.http"></a>
## tecs.http
```teal
tecs.http: http
```
<a id="tecs.input"></a>
## tecs.input
```teal
tecs.input: input
```
<a id="tecs.json"></a>
## tecs.json
```teal
tecs.json: json
```
<a id="tecs.layers"></a>
## tecs.layers
```teal
tecs.layers: layers
```
<a id="tecs.log"></a>
## tecs.log
```teal
tecs.log: log
```
<a id="tecs.materials"></a>
## tecs.materials
```teal
tecs.materials: materials
```
<a id="tecs.mcp"></a>
## tecs.mcp
```teal
tecs.mcp: mcp
```
<a id="tecs.net"></a>
## tecs.net
```teal
tecs.net: net
```
<a id="tecs.particles"></a>
## tecs.particles
```teal
tecs.particles: particles
```
<a id="tecs.paths"></a>
## tecs.paths
```teal
tecs.paths: paths
```
<a id="tecs.physics"></a>
## tecs.physics
```teal
tecs.physics: physics
```
<a id="tecs.proc"></a>
## tecs.proc
```teal
tecs.proc: proc
```
<a id="tecs.renderer"></a>
## tecs.renderer
```teal
tecs.renderer: renderer
```
<a id="tecs.sensors"></a>
## tecs.sensors
```teal
tecs.sensors: sensors
```
<a id="tecs.sequence"></a>
## tecs.sequence
```teal
tecs.sequence: sequence
```
<a id="tecs.sheet"></a>
## tecs.sheet
```teal
tecs.sheet: sheet
```
<a id="tecs.system"></a>
## tecs.system
```teal
tecs.system: system
```
<a id="tecs.text"></a>
## tecs.text
```teal
tecs.text: text
```
<a id="tecs.version"></a>
## tecs.version
```teal
tecs.version: string
```

Version of this build.
<a id="tecs.watch"></a>
## tecs.watch
```teal
tecs.watch: watch
```
<a id="tecs.window"></a>
## tecs.window
```teal
tecs.window: window
```
<a id="tecs.workers"></a>
## tecs.workers
```teal
tecs.workers: workers
```
## The ECS, in full

<a id="tecs.ecs.Archetype"></a>
## tecs.ecs.Archetype
```teal
type tecs.ecs.Archetype = types.Archetype
```
<a id="tecs.ecs.ArchetypeEntityObserver"></a>
## tecs.ecs.ArchetypeEntityObserver
```teal
type tecs.ecs.ArchetypeEntityObserver = types.ArchetypeEntityObserver
```
<a id="tecs.ecs.Bundle"></a>
## tecs.ecs.Bundle
```teal
type tecs.ecs.Bundle = types.Bundle
```
<a id="tecs.ecs.BundleDef"></a>
## tecs.ecs.BundleDef
```teal
type tecs.ecs.BundleDef = types.Bundle.Definition
```
<a id="tecs.ecs.Component"></a>
## tecs.ecs.Component
```teal
type tecs.ecs.Component = types.components.Component
```
<a id="tecs.ecs.ComponentOptions"></a>
## tecs.ecs.ComponentOptions
```teal
type tecs.ecs.ComponentOptions = generic<C is Component> types.components.ComponentOptions<C>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> | <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

<a id="tecs.ecs.Context"></a>
## tecs.ecs.Context
```teal
type tecs.ecs.Context = types.Context
```
<a id="tecs.ecs.DEFAULT_MAX_ENTITIES"></a>
## tecs.ecs.DEFAULT_MAX_ENTITIES
```teal
tecs.ecs.DEFAULT_MAX_ENTITIES: integer
```

Default `World.Config.maxEntities` when none is given (2^20).
<a id="tecs.ecs.DoubleArray"></a>
## tecs.ecs.DoubleArray
```teal
type tecs.ecs.DoubleArray = types.DoubleArray
```
<a id="tecs.ecs.Event"></a>
## tecs.ecs.Event
```teal
type tecs.ecs.Event = types.events.Event
```
<a id="tecs.ecs.EventInit"></a>
## tecs.ecs.EventInit
```teal
type tecs.ecs.EventInit = types.events.EventInit
```
<a id="tecs.ecs.EventListener"></a>
## tecs.ecs.EventListener
```teal
type tecs.ecs.EventListener = types.events.EventListener
```
<a id="tecs.ecs.FFIComponentOptions"></a>
## tecs.ecs.FFIComponentOptions
```teal
type tecs.ecs.FFIComponentOptions = generic<C is Component> types.components.FFIComponentOptions<C>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> | <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

<a id="tecs.ecs.FFIRelationshipOptions"></a>
## tecs.ecs.FFIRelationshipOptions
```teal
type tecs.ecs.FFIRelationshipOptions = generic<R is Relationship> types.components.FFIRelationshipOptions<R>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.ecs.Relationship">Relationship</a></code> |  |

<a id="tecs.ecs.FixedOverload"></a>
## tecs.ecs.FixedOverload
```teal
type tecs.ecs.FixedOverload = types.FixedOverload
```
<a id="tecs.ecs.Key"></a>
## tecs.ecs.Key
```teal
type tecs.ecs.Key = generic<T> types.Context.Key<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.ecs.MAX_ENTITIES"></a>
## tecs.ecs.MAX_ENTITIES
```teal
tecs.ecs.MAX_ENTITIES: integer
```

Absolute ceiling for `World.Config.maxEntities` (2^22 - 1 usable
slots; slot 0 is reserved by the entity-id format).
<a id="tecs.ecs.MessageBus"></a>
## tecs.ecs.MessageBus
```teal
type tecs.ecs.MessageBus = types.events.MessageBus
```
<a id="tecs.ecs.Phase"></a>
## tecs.ecs.Phase
```teal
type tecs.ecs.Phase = types.Phase
```
<a id="tecs.ecs.Pipeline"></a>
## tecs.ecs.Pipeline
```teal
type tecs.ecs.Pipeline = types.Pipeline
```
<a id="tecs.ecs.Plugin"></a>
## tecs.ecs.Plugin
```teal
type tecs.ecs.Plugin = types.Plugin
```
<a id="tecs.ecs.Query"></a>
## tecs.ecs.Query
```teal
type tecs.ecs.Query = types.Query
```
<a id="tecs.ecs.QueryCursor"></a>
## tecs.ecs.QueryCursor
```teal
type tecs.ecs.QueryCursor = types.Query.Cursor
```
<a id="tecs.ecs.QueryDescriptor"></a>
## tecs.ecs.QueryDescriptor
```teal
type tecs.ecs.QueryDescriptor = types.Query.Descriptor
```
<a id="tecs.ecs.Relationship"></a>
## tecs.ecs.Relationship
```teal
type tecs.ecs.Relationship = types.components.Relationship
```
<a id="tecs.ecs.RelationshipOptions"></a>
## tecs.ecs.RelationshipOptions
```teal
type tecs.ecs.RelationshipOptions = generic<R is Relationship> types.components.RelationshipOptions<R>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.ecs.Relationship">Relationship</a></code> |  |

<a id="tecs.ecs.ScalarComponent"></a>
## tecs.ecs.ScalarComponent
```teal
type tecs.ecs.ScalarComponent = generic<T> types.components.ScalarComponent<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.ecs.ScalarComponentOptions"></a>
## tecs.ecs.ScalarComponentOptions
```teal
type tecs.ecs.ScalarComponentOptions = generic<T> types.components.ScalarComponentOptions<T>
```
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

<a id="tecs.ecs.Snapshot"></a>
## tecs.ecs.Snapshot
```teal
type tecs.ecs.Snapshot = types.World.Snapshot
```
<a id="tecs.ecs.SnapshotComponentTableEntry"></a>
## tecs.ecs.SnapshotComponentTableEntry
```teal
type tecs.ecs.SnapshotComponentTableEntry = types.World.SnapshotComponentTableEntry
```
<a id="tecs.ecs.SnapshotHandler"></a>
## tecs.ecs.SnapshotHandler
```teal
type tecs.ecs.SnapshotHandler = types.World.SnapshotHandler
```
<a id="tecs.ecs.SnapshotOptions"></a>
## tecs.ecs.SnapshotOptions
```teal
type tecs.ecs.SnapshotOptions = types.World.SnapshotOptions
```
<a id="tecs.ecs.SnapshotOutput"></a>
## tecs.ecs.SnapshotOutput
```teal
type tecs.ecs.SnapshotOutput = types.World.SnapshotOutput
```
<a id="tecs.ecs.SnapshotPrelude"></a>
## tecs.ecs.SnapshotPrelude
```teal
type tecs.ecs.SnapshotPrelude = types.World.SnapshotPrelude
```
<a id="tecs.ecs.StatePolicy"></a>
## tecs.ecs.StatePolicy
```teal
type tecs.ecs.StatePolicy = types.StatePolicy
```
<a id="tecs.ecs.Stats"></a>
## tecs.ecs.Stats
```teal
type tecs.ecs.Stats = types.World.Stats
```
<a id="tecs.ecs.System"></a>
## tecs.ecs.System
```teal
type tecs.ecs.System = types.System
```
<a id="tecs.ecs.SystemConfig"></a>
## tecs.ecs.SystemConfig
```teal
type tecs.ecs.SystemConfig = types.SystemConfig
```
<a id="tecs.ecs.TagComponentOptions"></a>
## tecs.ecs.TagComponentOptions
```teal
type tecs.ecs.TagComponentOptions = types.components.TagComponentOptions
```
<a id="tecs.ecs.World"></a>
## tecs.ecs.World
```teal
type tecs.ecs.World = types.World
```
<a id="tecs.ecs.builtins"></a>
## tecs.ecs.builtins
```teal
tecs.ecs.builtins: builtins
```

The components every world carries: `ChildOf`, `Transform`, `TTL`,
`Paused`, `Disabled` and the state transition events.
<a id="tecs.ecs.componentByName"></a>
## tecs.ecs.componentByName
```teal
function tecs.ecs.componentByName(string): Component
```

The component registered under `name`, or nil.

The registry is process-wide, because a component's id is allocated
once at registration and every world that carries one agrees on it. So
this answers what can be named, and a world answers what is present.

Finds a dense relationship instance by its stamped name as well, which
`declaredComponents` leaves out.
#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>string</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

<a id="tecs.ecs.declaredComponents"></a>
## tecs.ecs.declaredComponents
```teal
function tecs.ecs.declaredComponents(): {string : Component}
```

Every component declared in this process, name to component.

A fresh table per call, so the registry itself stays unwritable from
outside registration.

Dense relationship instances are left out. One is registered per target
under a stamped `Rel->42` name, so a world with many edges has many of
them, and none of them is a component anybody declared.
#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>{string : <a href="#tecs.ecs.Component">Component</a>}</code> |  |

<a id="tecs.ecs.findKey"></a>
## tecs.ecs.findKey
```teal
function tecs.ecs.findKey<T>(name: string): Key<T> | nil
```

Find a named key created by `newKey`. Returns nil when unknown.

#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>string</code> | <code v-pre>name</code> | The name the key was created with. |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Key">Key</a>&lt;T&gt; \| nil</code> |  |

<a id="tecs.ecs.getComponentById"></a>
## tecs.ecs.getComponentById
```teal
function tecs.ecs.getComponentById(integer): Component
```

Look up a registered component by numeric ID.
#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>integer</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

<a id="tecs.ecs.listKeys"></a>
## tecs.ecs.listKeys
```teal
function tecs.ecs.listKeys(): {string : integer}
```

All named keys, as a fresh name -> numeric key id table.
#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>{string : integer}</code> |  |

<a id="tecs.ecs.newComponent"></a>
## tecs.ecs.newComponent
```teal
function tecs.ecs.newComponent<C is Component>(ComponentOptions<C>): C
```

Creates and registers a new table component. See `ecs.ComponentOptions`
and the component docs for the shared `fields` / `defaults` / `init` /
`.new` model.
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> | <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.ComponentOptions">ComponentOptions</a>&lt;C&gt;</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>C</code> |  |

<a id="tecs.ecs.newContext"></a>
## tecs.ecs.newContext
```teal
function tecs.ecs.newContext(): Context
```

Create a new Context instance.
#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Context">Context</a></code> |  |

<a id="tecs.ecs.newEvent"></a>
## tecs.ecs.newEvent
```teal
function tecs.ecs.newEvent<E is Event>(event: E)
```

Configure an event to have an appropriate __call based constructor.

#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>E</code> | <code v-pre><a href="#tecs.ecs.Event">Event</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>E</code> | <code v-pre>event</code> | The event instance to configure. |

<a id="tecs.ecs.newFFIComponent"></a>
## tecs.ecs.newFFIComponent
```teal
function tecs.ecs.newFFIComponent<C is Component>(FFIComponentOptions<C>): C
```

Creates and registers an FFI-based component. Same constructor model
as `newComponent`, but the base instance is an FFI struct.
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>C</code> | <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.FFIComponentOptions">FFIComponentOptions</a>&lt;C&gt;</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>C</code> |  |

<a id="tecs.ecs.newFFIEvent"></a>
## tecs.ecs.newFFIEvent
```teal
function tecs.ecs.newFFIEvent<E is Event>(event: E, fields: {{string, string}}, structName: string)
```

Configure an FFI event to have an appropriate __call based constructor.

#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>E</code> | <code v-pre><a href="#tecs.ecs.Event">Event</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>E</code> | <code v-pre>event</code> | The event type to configure. |
| <code v-pre><span v-pre>{{</span>string, string}}</code> | <code v-pre>fields</code> | Field definitions in format <span v-pre>{{</span>"name", "type"}, ...} |
| <code v-pre>string</code> | <code v-pre>structName</code> | Optional struct name (auto-generated if not provided) |

<a id="tecs.ecs.newFFIRelationship"></a>
## tecs.ecs.newFFIRelationship
```teal
function tecs.ecs.newFFIRelationship<R is Relationship>(FFIRelationshipOptions<R>): R
```

Create an FFI-backed relationship with data fields.
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.ecs.Relationship">Relationship</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.FFIRelationshipOptions">FFIRelationshipOptions</a>&lt;R&gt;</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>R</code> |  |

<a id="tecs.ecs.newKey"></a>
## tecs.ecs.newKey
```teal
function tecs.ecs.newKey<T>(name: string, forType: T): Key<T>
```

A typed key for `world.resources`, and the way an engine module stores
something scoped to one world.

Always pass a name: named keys are discoverable at runtime through
`findKey`/`listKeys` and the tooling built on them (the MCP `inspect`
tool, `tecs info --keys`), and a named key is a stable identity --
calling `newKey` again with the same name returns the same key, so
values keyed by it survive hot reload. An unnamed key works but logs a
warning and stays invisible to name-based lookups.

`forType` carries the phantom type, because Teal has no explicit type
argument at a call site: pass `nil as T` for `world.resources[key]` to
read as a `T` rather than as `any`.

#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>string</code> | <code v-pre>name</code> | A stable, unique name for the key, e.g. "game.state". |
| <code v-pre>T</code> | <code v-pre>forType</code> | Optional type parameter for type inference. |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Key">Key</a>&lt;T&gt;</code> | A new key that can be used to store values of type T. |

<a id="tecs.ecs.newMessageBus"></a>
## tecs.ecs.newMessageBus
```teal
function tecs.ecs.newMessageBus(): MessageBus
```

Create a new address-based event message bus.
#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.MessageBus">MessageBus</a></code> |  |

<a id="tecs.ecs.newRelationship"></a>
## tecs.ecs.newRelationship
```teal
function tecs.ecs.newRelationship<R is Relationship>(RelationshipOptions<R>): R
```

Creates and registers a new relationship component. See the relationship
docs for target/exclusive/sparse semantics. A target-only relationship
(name plus flags, no `container`/`fields`) is backed by a compact FFI
struct automatically; add `container`/`fields` for relationships that
carry data.
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>R</code> | <code v-pre><a href="#tecs.ecs.Relationship">Relationship</a></code> |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.RelationshipOptions">RelationshipOptions</a>&lt;R&gt;</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre>R</code> |  |

<a id="tecs.ecs.newScalarComponent"></a>
## tecs.ecs.newScalarComponent
```teal
function tecs.ecs.newScalarComponent<T>(ScalarComponentOptions<T>): ScalarComponent<T>
```

Creates and registers a new scalar component.
#### Type Parameters

| Name | Constraint | Description |
| --- | --- | --- |
| <code v-pre>T</code> |  |  |

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.ScalarComponentOptions">ScalarComponentOptions</a>&lt;T&gt;</code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.ScalarComponent">ScalarComponent</a>&lt;T&gt;</code> |  |

<a id="tecs.ecs.newTagComponent"></a>
## tecs.ecs.newTagComponent
```teal
function tecs.ecs.newTagComponent(TagComponentOptions): Component
```

Creates and registers a new tag component that uses bitset storage for efficiency.
#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre><a href="#tecs.ecs.TagComponentOptions">TagComponentOptions</a></code> |  |  |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.Component">Component</a></code> |  |

<a id="tecs.ecs.newWorld"></a>
## tecs.ecs.newWorld
```teal
function tecs.ecs.newWorld(config: types.World.Config): World
```

Create a new World.

#### Parameters

| Type | Name | Description |
| --- | --- | --- |
| <code v-pre>types.World.Config</code> | <code v-pre>config</code> | Optional world configuration. |

#### Returns

| Type | Description |
| --- | --- |
| <code v-pre><a href="#tecs.ecs.World">World</a></code> | the created World. |

<a id="tecs.ecs.phases"></a>
## tecs.ecs.phases
```teal
tecs.ecs.phases: phases
```

The phases a system can be scheduled into.
<a id="tecs.ecs.random"></a>
## tecs.ecs.random
```teal
tecs.ecs.random: random
```

Seeded generation, in named streams a snapshot carries.
<a id="tecs.ecs.runif"></a>
## tecs.ecs.runif
```teal
tecs.ecs.runif: runIfHelpers
```

Composable run conditions, for `SystemConfig.runIf`.
