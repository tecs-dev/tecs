import{_ as t,o as a,c as i,ag as o}from"./chunks/framework.BPmc_f4o.js";const g=JSON.parse('{"title":"tecs.application.Application","description":"The application object an entry file returns and the host drives: its config, what it owns, and the frame","frontmatter":{"description":"The application object an entry file returns and the host drives: its config, what it owns, and the frame","outline":[2,3]},"headers":[],"relativePath":"modules/application.md","filePath":"modules/application.md"}'),n={name:"modules/application.md"};function s(c,e,p,r,d,l){return a(),i("div",null,[...e[0]||(e[0]=[o(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /modules/application.md for this page in Markdown format</div><h1 id="tecs-application-application" tabindex="-1">tecs.application.Application <a class="header-anchor" href="#tecs-application-application" aria-label="Permalink to &quot;tecs.application.Application&quot;">​</a></h1><p><code>tecs.application.Application</code> is the lifecycle the C host drives. An entry file ends with <code>return tecs.application.create(config)</code>, and the host calls into the returned object to initialise, for each event, for each iteration, and to shut down.</p><p>It is not a function that runs until done. A platform that never hands control back has no loop to block in, so the loop lives below Lua and the application is what it calls.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> require</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">tecs</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">return</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">application</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">create</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">({</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    window </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= { </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">title </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">Hello</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">width </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">1280</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">height </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">960</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> },</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    plugin </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(world: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">tecs</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">World</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, app: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">tecs</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">application</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Application</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">        -- everything the game registers goes here</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    end</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">})</span></span></code></pre></div><h2 id="one-way-in" tabindex="-1">One way in <a class="header-anchor" href="#one-way-in" aria-label="Permalink to &quot;One way in&quot;">​</a></h2><p>A game&#39;s own code runs in systems and observers, and <code>config.plugin</code> is the only entry point. It is handed the world and the application, and registers what it wants to happen; the world runs it.</p><p>Nothing calls back into a game per frame or per event, because the world already does both and does them better. A system&#39;s order is declared by its phase, both systems and observers are visible to the debug server, and both run inside a guard that keeps the process alive and inspectable after the game throws. So the four lifecycle points an engine usually hands a game map onto what the ECS already had:</p><ul><li><strong>Startup</strong> — <code>PreStartup</code>, <code>Startup</code>, <code>PostStartup</code>, run once at the end of initialisation.</li><li><strong>Per frame</strong> — the ordinary phases, run by <code>world:update</code>.</li><li><strong>Per event</strong> — <code>world:observe(0, tecs.events.on.&lt;kind&gt;, handler)</code>.</li><li><strong>Teardown</strong> — <code>PreShutdown</code>, <code>Shutdown</code>, <code>PostShutdown</code>, run once before anything is destroyed.</li></ul><p>One entry point rather than a list, because composing plugins is something the world already does: <code>world:addPlugin</code> takes an <a href="/ecs/plugins.html"><code>ecs.Plugin</code></a> and is how the engine installs its own, so a game with several calls it from in here and needs no second mechanism.</p><p>The world comes first in the signature because every plugin the world takes is <code>function(world)</code>, so this reads as that shape with one more thing, and code moves between here and a delegated plugin without a silent argument swap.</p><p>The plugin runs at the end of initialisation, after every engine subsystem is installed and before the startup phases run, so a <code>Startup</code> system it registers runs on this initialisation rather than the next one.</p><h2 id="the-config" tabindex="-1">The config <a class="header-anchor" href="#the-config" aria-label="Permalink to &quot;The config&quot;">​</a></h2><p>Every field is optional, including <code>plugin</code>. Nothing here is validated by the application: values are passed through to the object that owns them, so a timestep of zero is refused by the world rather than in two places that could disagree.</p><h3 id="the-window-and-the-device" tabindex="-1">The window and the device <a class="header-anchor" href="#the-window-and-the-device" aria-label="Permalink to &quot;The window and the device&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>What it does</th></tr></thead><tbody><tr><td><code>window</code></td><td><a href="/modules/window.html"><code>Window.Options</code></a>, passed through whole</td></tr><tr><td><code>framesInFlight</code></td><td>Frames the device may have outstanding. Omitted leaves the device&#39;s own default</td></tr><tr><td><code>presentMode</code></td><td>How the swapchain presents. Omitted leaves the device&#39;s own default</td></tr><tr><td><code>ambientLight</code></td><td>Light every surface receives before any light entity contributes. Defaults white</td></tr></tbody></table><p><code>ambientLight</code> defaults to white rather than a dim grey because a scene that has placed no lights is the first thing anyone builds, and it should look like what it is: sprites at their own colour, with lights adding on top. A game doing its own lighting turns this down to the level it wants unlit parts of the scene to sit at.</p><h3 id="the-world-and-the-renderer" tabindex="-1">The world and the renderer <a class="header-anchor" href="#the-world-and-the-renderer" aria-label="Permalink to &quot;The world and the renderer&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>What it does</th></tr></thead><tbody><tr><td><code>maxEntities</code></td><td><code>2^20</code></td><td>Entity slots the world is sized for. <code>tecs.ecs.MAX_ENTITIES</code> is the ceiling</td></tr><tr><td><code>capacity</code></td><td><code>65536</code></td><td>Instances the renderer&#39;s buffers are sized for</td></tr><tr><td><code>timestep</code></td><td><code>1/60</code></td><td>Seconds one fixed step covers</td></tr><tr><td><code>fixedMaxSteps</code></td><td><code>10</code></td><td>The most fixed steps one update runs before the overload policy applies</td></tr><tr><td><code>fixedOverload</code></td><td><code>&quot;drop&quot;</code></td><td>What becomes of the catch-up that did not fit; <code>&quot;accumulate&quot;</code> keeps it</td></tr><tr><td><code>reserveRuns</code></td><td><code>false</code></td><td>Give each archetype a run with room to grow rather than packing them</td></tr></tbody></table><p><code>maxEntities</code> counts concurrent slots rather than lifetime spawns: a slot is given back on despawn and reused. The arena is preallocated for this many, so it is a memory decision as much as a limit.</p><p><code>capacity</code> is a ceiling and not a hint. An instance is a row the GPU draws: every renderable entity is one, and so is every glyph of every text, since a glyph is an entity like any other. Rows past the ceiling are dropped rather than growing a buffer mid-frame, and <code>app.renderer.dropped</code> counts them. It is sized independently of <code>maxEntities</code> because the two count different things: a world full of entities carrying no <code>Renderable</code> needs no instances at all, and one text entity needs one per character.</p><p><code>reserveRuns</code> is a tuning setting, not a debugging one. Packed, a run cannot grow without shifting every run after it, so a spawn anywhere rewrites every instance in the scene. Reserved, a run grows into its own slack and the archetypes around it are left alone. What it costs is that the cull is dispatched over the slack as well as over the rows, and that reserving needs headroom: a <code>capacity</code> sized to exactly the population leaves nothing to reserve out of and the setting does nothing. Worth it for a scene that spawns into one archetype while most of the world sits in others; worth nothing for a scene held in a single archetype.</p><h3 id="sound-logging-and-diagnostics" tabindex="-1">Sound, logging and diagnostics <a class="header-anchor" href="#sound-logging-and-diagnostics" aria-label="Permalink to &quot;Sound, logging and diagnostics&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>What it does</th></tr></thead><tbody><tr><td><code>audio</code></td><td><a href="/modules/audio.html"><code>Audio.Config</code></a>. Omitted opens the platform&#39;s default device</td></tr><tr><td><code>logFile</code></td><td>Log file under the writable root, as JSON Lines, beside the platform&#39;s destination</td></tr><tr><td><code>logLevel</code></td><td>Lowest priority that reaches the log at all, from <a href="/modules/log.html"><code>tecs.log</code></a></td></tr><tr><td><code>checkpoint</code></td><td>File the staged checkpoint is written to when the platform backgrounds us</td></tr><tr><td><code>mcpPort</code></td><td>Port for the <a href="/modules/mcp.html">debug server</a>. Omitted means no server</td></tr><tr><td><code>watch</code></td><td><a href="/modules/filesystem/watch.html"><code>watch.Config</code></a> for content hot reload. Omitted means no watcher</td></tr><tr><td><code>debug</code></td><td>Marks this a development run</td></tr><tr><td><code>debugMaxFrames</code></td><td>Stops after this many iterations</td></tr></tbody></table><p><code>mcpPort</code> and <code>watch</code> are omitted by default because a game should no more open a socket nobody asked for than poll a filesystem nobody asked for. A release refuses the watcher outright.</p><p><code>logFile</code> follows the thing that reads it. Omitted, one is written when <code>mcpPort</code> or <code>debug</code> is set and not otherwise: the file exists for the debug server, which is what makes its <code>get_logs</code> a seek and a read rather than a ring buffer kept in the process. Nothing is lost by the default, because SDL already dispatches to a destination a human can read on every platform. A shipped game that wants a crash log in the field names one, since where a game writes user data is the game&#39;s decision.</p><p><code>logLevel</code> is separate from <code>logFile</code> beside it: one decides volume, the other durability. A game wanting everything kept and little of it shown sets both.</p><p><code>debug</code> is read by two things. The device is created with its validation layers on, and the log file above is written without being asked for. It is also one of the settings <a href="#crashes"><code>clearCrash</code></a> looks at.</p><p><code>debugMaxFrames</code> lets an automated run drive a real window to completion without a human closing it.</p><h2 id="what-it-owns" tabindex="-1">What it owns <a class="header-anchor" href="#what-it-owns" aria-label="Permalink to &quot;What it owns&quot;">​</a></h2><p>These are fields on the application a plugin is handed, so nothing has to be looked up:</p><ul><li><code>app.window</code> — <a href="/modules/window.html"><code>Window</code></a></li><li><code>app.device</code> — the GPU device</li><li><code>app.world</code> — the <a href="/ecs/world.html">world</a></li><li><code>app.renderer</code> — <a href="/modules/gfx/"><code>Renderer</code></a></li><li><code>app.input</code> — <a href="/modules/input.html"><code>Input</code></a></li><li><code>app.audio</code> — <a href="/modules/audio.html"><code>Audio</code></a></li><li><code>app.mcp</code> — the debug server, when <code>mcpPort</code> asked for one</li></ul><p>The application is handed to the plugin rather than looked up, so there is no <code>Application.of</code> and no <code>Renderer.of</code> to call: a system that needs either captures it from here when the plugin registers the system, and what it captures cannot be nil. <a href="/modules/audio.html#of"><code>tecs.audio.Audio.of</code></a> is the one accessor of that kind that exists, because the debug tools reach the mixer holding only a world.</p><p>And four values it keeps about the run:</p><ul><li><code>app.quitRequested</code> — set it to <code>true</code> to leave the loop at the end of the current iteration</li><li><code>app.suspended</code> — true while the platform has the application backgrounded. The loop still runs; simulation and rendering do not</li><li><code>app.elapsed</code> — seconds of <strong>simulated</strong> time, advanced by the frame dt, so a replay reproduces it exactly and a crashed run does not fold the length of the fix into it</li><li><code>app.frame</code> — iterations completed</li></ul><h2 id="what-one-iteration-does" tabindex="-1">What one iteration does <a class="header-anchor" href="#what-one-iteration-does" aria-label="Permalink to &quot;What one iteration does&quot;">​</a></h2><p>In order, and the order is the contract:</p><ol><li>Read the clock for <code>dt</code>, then <code>input:beginFrame()</code>.</li><li>Drain every event SDL delivered since the previous iteration. Each is folded into engine state and then emitted on the world&#39;s bus, so an observer reading <code>app.input</code> inside its handler sees the press it is being told about rather than the state before it. Observers see every event, including ones the engine acted on: an engine that consumed events would leave a game unable to tell an event it never received from one it mishandled.</li><li>Return early if <code>quitRequested</code> was set.</li><li><code>assets.update()</code>, <code>proc.update()</code>, <code>watch.poll()</code> — these run outside the suspension and crash guards, because a decode that finished while backgrounded is finished, a child that exited has exited, and a shader edited to fix the crash is exactly what a stopped process is waiting for.</li><li>If not suspended: poll the debug server, then, if the game has not crashed, advance <code>elapsed</code> and run the guarded frame — <code>world:update(dt)</code>, acquire a frame, <code>renderer:render(frame)</code>, <code>frame:submit()</code>.</li><li><code>audio:update(dt)</code>, also outside both guards: a sound that sticks because the game crashed or the window went to the background is a second failure on top of the first.</li></ol><p>An observer is not a system, and the difference matters. The drain in step 2 runs before <code>world:update</code>, so an observer fires ahead of every system in the frame the event belongs to and outside every phase: no fixed step, no pause or state gating, and mutations apply at once rather than deferring to a phase boundary. A game wanting a reaction in phase does what <code>Input</code> does, which is fold the event into something a system reads.</p><h2 id="checkpoints" tabindex="-1">Checkpoints <a class="header-anchor" href="#checkpoints" aria-label="Permalink to &quot;Checkpoints&quot;">​</a></h2><p>Mobile platforms give an application a few seconds&#39; notice before backgrounding it, and rather less on Android. A world is not serialisable inside any of that at this project&#39;s scale, so this takes bytes rather than a callback that produces them.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">app:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">stageCheckpoint</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(save)   </span><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">-- from an ordinary system, on an ordinary frame</span></span></code></pre></div><p>Nothing is written there. The bytes are held, and the engine writes them at the one moment the platform gives. By the time they exist the expensive half has already happened on a frame that could afford it, which a callback would let a game postpone into exactly the moment it cannot. The host times the hook and says so past 250 ms.</p><p>Staging again replaces what was staged; there is one checkpoint, not a queue. Staging and then backgrounding twice writes once, because a write nothing has changed since is a second chance to be interrupted for nothing.</p><p><code>stageCheckpoint</code> raises when no <code>checkpoint</code> file was configured, because silently holding bytes that will never be written is the one failure a game would not notice.</p><table tabindex="0"><thead><tr><th>Method</th><th>What it answers</th></tr></thead><tbody><tr><td><code>app:stageCheckpoint(b)</code></td><td>Hands the engine the bytes to write when the platform backgrounds us</td></tr><tr><td><code>app:readCheckpoint()</code></td><td>The bytes the last run left, or nil when there are none</td></tr><tr><td><code>app:checkpointPath()</code></td><td>Where the checkpoint is written, or nil when none was configured</td></tr></tbody></table><p>Read the checkpoint while building the world, which is what the plugin and the startup phases are for. Nil covers every reason there is nothing to resume from — a first run, a game that never staged anything, a file the player deleted — and none of those is an error, so none of them raises.</p><h2 id="crashes" tabindex="-1">Crashes <a class="header-anchor" href="#crashes" aria-label="Permalink to &quot;Crashes&quot;">​</a></h2><p>Every entry into a game&#39;s own code is guarded. When a system, an observer, the plugin or a startup phase throws, the traceback is recorded rather than propagated: the loop keeps draining events so the window still closes, and keeps answering the debug server so an agent that was watching gets the traceback rather than a refused connection. Engine code is deliberately not guarded, because an engine that throws is a bug here and swallowing it would hide it.</p><p>The first traceback wins. A throw after it is usually the consequence rather than the explanation. One of the later ones is expected: teardown still runs <code>world:shutdown</code> on a crashed world, because a game that acquired something before it threw still has to be given the chance to give it back, and a <code>Shutdown</code> system reaching for what the crash left half built throws in its turn.</p><p>While the game is crashed, events are still folded into engine state and no longer emitted on the world&#39;s bus. Quit still closes the window, <code>Input</code> still tracks what is held and the debug server&#39;s <code>send_event</code> still reaches both, but no observer runs until <code>clearCrash</code> does, because a game whose systems have stopped is not in a state to handle an event either and an observer that throws produces the one traceback nobody needed.</p><ul><li><code>app:crashed()</code> — the gameplay traceback, or nil while the game is healthy.</li><li><code>app:clearCrash()</code> — returns whether the loop resumed, and why it did not.</li></ul><p><code>clearCrash</code> is a debugging affordance, not fault tolerance, and its whole contract is one sentence: <strong>another frame to look at and a chance to reload; the world may be inconsistent, reload before trusting it.</strong> What the guard put back are the engine&#39;s invariants, which are the ones it can name — the command buffer is resolved, the passes are ended, the world is applying mutations again. A game&#39;s invariants are not restored and cannot be, because a system that threw halfway through its query left some entities updated and some not, and nothing here knows which half or what either half meant.</p><p>What stays on screen is whatever the frame had drawn before it threw, which for a throw ahead of present is whatever the swapchain texture last held. Recovery does not clear it, because what the game looks like is not a decision recovery gets to make.</p><p>It refuses on two gates:</p><ul><li><strong>This build is not a development one.</strong> With none of <code>debug</code>, <code>mcpPort</code> or <code>watch</code> set, the first crash latches and stays latched. That is the right shipped behaviour: there is nobody there to read the frame, and carrying on with a world that is provably inconsistent is how one bug becomes a corrupted save.</li><li><strong>Recovery was not clean.</strong> If a pass had to be force-ended or a frame cancelled rather than submitted, the device is not in the state the engine intended, and the next frame would be recorded against a device nobody can describe. That refuses at any setting.</li></ul><p>Its callers are the debug server&#39;s <code>clear_crash</code> tool and the file watcher&#39;s reloads. Both mean the same thing: something about the run has changed, try it again.</p><p>Physics resumability is a separate and open question: a solver stepped out of is not obviously a solver that can be stepped again.</p><h2 id="the-platform-lifecycle" tabindex="-1">The platform lifecycle <a class="header-anchor" href="#the-platform-lifecycle" aria-label="Permalink to &quot;The platform lifecycle&quot;">​</a></h2><p>SDL dispatches the platform lifecycle from its event watcher rather than queueing it, and the host calls one method per event at the instant it arrives, because on Android the process blocks as soon as backgrounding has been sent and the next iteration is after the resume.</p><p>The engine answers five of those: low memory triggers a full collection, backgrounding sets <code>suspended</code> and flushes the checkpoint, entering the background waits for the device to go idle, returning to the foreground lifts the suspension, and termination writes the checkpoint one last time.</p><p>Every one of them is also queued on the bus, so a game reads the transitions as ordinary events — <code>tecs.events.on.lowMemory</code>, <code>tecs.events.on.appWillEnterForeground</code> and the rest. The hook is where the engine meets a deadline the event stream cannot; a game reads the event. The one thing a game cannot do that way is save state on being backgrounded, because there may be no later drain to do it in, and <code>stageCheckpoint</code> exists for exactly that.</p><p>Releasing the graphics device on suspension is <strong>not</strong> done, and the gap is stated rather than half closed. Three things would have to be true first: every GPU handle in the process would have to be reachable from one place to be released and recreated, the image array would have to be refillable when the pixels behind it are released at <code>Renderer:registerImage</code>, and it would have to be testable, which it is not while none of these events fires on the platforms the suite runs on.</p><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><p>Every function and type this module carries, rendered from <code>src/tecs/Application.tl</code>.</p><p><a id="tecs.application.Application.Config"></a></p><h3 id="tecs-application-application-config" tabindex="-1">tecs.application.Application.Config <a class="header-anchor" href="#tecs-application-application-config" aria-label="Permalink to &quot;tecs.application.Application.Config&quot;">​</a></h3><pre><code>record <a href="#tecs.application.Application.Config">tecs.application.Application.Config</a>
</code></pre><p><a id="tecs.application.Application.Config.window"></a></p><h3 id="tecs-application-application-config-window" tabindex="-1">tecs.application.Application.Config.window <a class="header-anchor" href="#tecs-application-application-config-window" aria-label="Permalink to &quot;tecs.application.Application.Config.window&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.window">tecs.application.Application.Config.window</a>: Window.Options
</code></pre><p>The window to open, as <code>Window.Options</code> describes it. Passed through whole rather than copied field by field, so everything a window can be created with is reachable from here and a field added there needs nothing added here. <a id="tecs.application.Application.Config.debug"></a></p><h3 id="tecs-application-application-config-debug" tabindex="-1">tecs.application.Application.Config.debug <a class="header-anchor" href="#tecs-application-application-config-debug" aria-label="Permalink to &quot;tecs.application.Application.Config.debug&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.debug">tecs.application.Application.Config.debug</a>: boolean
</code></pre><p>Marks this run as a development one, which two things read.</p><p>The device is created with its validation layers on, and the log file below is written without being asked for. It is also one of the three settings <code>clearCrash</code> looks at, beside <code>mcpPort</code> and <code>watch</code>, to decide whether resuming after a gameplay crash is something this build does at all. <a id="tecs.application.Application.Config.framesInFlight"></a></p><h3 id="tecs-application-application-config-framesinflight" tabindex="-1">tecs.application.Application.Config.framesInFlight <a class="header-anchor" href="#tecs-application-application-config-framesinflight" aria-label="Permalink to &quot;tecs.application.Application.Config.framesInFlight&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.framesInFlight">tecs.application.Application.Config.framesInFlight</a>: integer
</code></pre><p><a id="tecs.application.Application.Config.presentMode"></a></p><h3 id="tecs-application-application-config-presentmode" tabindex="-1">tecs.application.Application.Config.presentMode <a class="header-anchor" href="#tecs-application-application-config-presentmode" aria-label="Permalink to &quot;tecs.application.Application.Config.presentMode&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.presentMode">tecs.application.Application.Config.presentMode</a>: string
</code></pre><p><a id="tecs.application.Application.Config.ambientLight"></a></p><h3 id="tecs-application-application-config-ambientlight" tabindex="-1">tecs.application.Application.Config.ambientLight <a class="header-anchor" href="#tecs-application-application-config-ambientlight" aria-label="Permalink to &quot;tecs.application.Application.Config.ambientLight&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.ambientLight">tecs.application.Application.Config.ambientLight</a>: {number}
</code></pre><p>Light every surface receives before any light entity contributes, as red, green and blue. Defaults to white.</p><p>White rather than a dim grey, because a scene that has placed no lights is the first thing anyone builds and it should look like what it is: sprites at their own colour, with lights adding on top of them. A game doing its own lighting turns this down to the level it wants the unlit parts of the scene to sit at. <a id="tecs.application.Application.Config.audio"></a></p><h3 id="tecs-application-application-config-audio" tabindex="-1">tecs.application.Application.Config.audio <a class="header-anchor" href="#tecs-application-application-config-audio" aria-label="Permalink to &quot;tecs.application.Application.Config.audio&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.audio">tecs.application.Application.Config.audio</a>: Audio.Config
</code></pre><p>Sound output. Omitted takes the defaults, which open the platform&#39;s default device. <a id="tecs.application.Application.Config.logFile"></a></p><h3 id="tecs-application-application-config-logfile" tabindex="-1">tecs.application.Application.Config.logFile <a class="header-anchor" href="#tecs-application-application-config-logfile" aria-label="Permalink to &quot;tecs.application.Application.Config.logFile&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.logFile">tecs.application.Application.Config.logFile</a>: string
</code></pre><p>Log file name, under the writable root, written as JSON Lines beside the platform&#39;s own destination rather than instead of it.</p><p>Omitted, one is written when <code>mcpPort</code> or <code>debug</code> is set and not otherwise. The file exists for the debug server, which is what makes <code>get_logs</code> a seek and a read rather than a ring buffer kept in the process, so it follows the thing that reads it: a game should no more write a file nobody asked for than open a socket nobody asked for. Nothing is lost by that, because SDL already dispatches to a destination a human can read on every platform; see <code>log</code>. A shipped game that wants a crash log in the field names one here, since where a game writes user data is the game&#39;s decision and not the engine&#39;s. <a id="tecs.application.Application.Config.logLevel"></a></p><h3 id="tecs-application-application-config-loglevel" tabindex="-1">tecs.application.Application.Config.logLevel <a class="header-anchor" href="#tecs-application-application-config-loglevel" aria-label="Permalink to &quot;tecs.application.Application.Config.logLevel&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.logLevel">tecs.application.Application.Config.logLevel</a>: integer
</code></pre><p>Lowest priority that reaches the log at all, as one of <code>log.TRACE</code>, <code>log.VERBOSE</code>, <code>log.DEBUG</code>, <code>log.INFO</code>, <code>log.WARN</code>, <code>log.ERROR</code> or <code>log.CRITICAL</code>. Omitted leaves SDL&#39;s own default.</p><p>Applies to every category, ours and SDL&#39;s, and it is set before anything here has had a chance to say something. Separate from <code>logFile</code> above, which decides durability rather than volume: a game wanting everything kept and little of it shown sets both. <a id="tecs.application.Application.Config.checkpoint"></a></p><h3 id="tecs-application-application-config-checkpoint" tabindex="-1">tecs.application.Application.Config.checkpoint <a class="header-anchor" href="#tecs-application-application-config-checkpoint" aria-label="Permalink to &quot;tecs.application.Application.Config.checkpoint&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.checkpoint">tecs.application.Application.Config.checkpoint</a>: string
</code></pre><p>File name, under the writable root, that a checkpoint is written to when the platform backgrounds or terminates the application. Omitted means no checkpoint, and <code>stageCheckpoint</code> says so.</p><p>See <code>Application:stageCheckpoint</code> for what a game has to do, which is the part that matters. <a id="tecs.application.Application.Config.mcpPort"></a></p><h3 id="tecs-application-application-config-mcpport" tabindex="-1">tecs.application.Application.Config.mcpPort <a class="header-anchor" href="#tecs-application-application-config-mcpport" aria-label="Permalink to &quot;tecs.application.Application.Config.mcpPort&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.mcpPort">tecs.application.Application.Config.mcpPort</a>: integer
</code></pre><p>Port for the MCP server. Omitted means no server, since a game should not open a socket nobody asked for. <a id="tecs.application.Application.Config.watch"></a></p><h3 id="tecs-application-application-config-watch" tabindex="-1">tecs.application.Application.Config.watch <a class="header-anchor" href="#tecs-application-application-config-watch" aria-label="Permalink to &quot;tecs.application.Application.Config.watch&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.watch">tecs.application.Application.Config.watch</a>: watch.Config
</code></pre><p>Watches the content files this run has loaded and reloads them when they change. Omitted means no watcher, on the same footing as the server above: a poll of the filesystem is not something to start because a build happened to be able to. A release refuses it. <a id="tecs.application.Application.Config.capacity"></a></p><h3 id="tecs-application-application-config-capacity" tabindex="-1">tecs.application.Application.Config.capacity <a class="header-anchor" href="#tecs-application-application-config-capacity" aria-label="Permalink to &quot;tecs.application.Application.Config.capacity&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.capacity">tecs.application.Application.Config.capacity</a>: integer
</code></pre><p>Instances the renderer&#39;s buffers are sized for. Defaults to 65536.</p><p>An instance is a row the GPU draws: every renderable entity is one, and so is every glyph of every text, since a glyph is an entity like any other. This is a ceiling rather than a hint. Rows past it are dropped rather than growing a buffer mid-frame, and <code>renderer.dropped</code> counts them.</p><p>Sized independently of <code>maxEntities</code> beside it, because the two count different things: a world full of entities that carry no <code>Renderable</code> needs no instances at all, and one text entity needs one instance per character. <a id="tecs.application.Application.Config.timestep"></a></p><h3 id="tecs-application-application-config-timestep" tabindex="-1">tecs.application.Application.Config.timestep <a class="header-anchor" href="#tecs-application-application-config-timestep" aria-label="Permalink to &quot;tecs.application.Application.Config.timestep&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.timestep">tecs.application.Application.Config.timestep</a>: number
</code></pre><p>Seconds one fixed step covers. Must be greater than zero. Defaults to 1/60. <a id="tecs.application.Application.Config.fixedMaxSteps"></a></p><h3 id="tecs-application-application-config-fixedmaxsteps" tabindex="-1">tecs.application.Application.Config.fixedMaxSteps <a class="header-anchor" href="#tecs-application-application-config-fixedmaxsteps" aria-label="Permalink to &quot;tecs.application.Application.Config.fixedMaxSteps&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.fixedMaxSteps">tecs.application.Application.Config.fixedMaxSteps</a>: integer
</code></pre><p>The most fixed steps one update runs before the overload policy applies. Must be a positive integer. Defaults to 10. <a id="tecs.application.Application.Config.fixedOverload"></a></p><h3 id="tecs-application-application-config-fixedoverload" tabindex="-1">tecs.application.Application.Config.fixedOverload <a class="header-anchor" href="#tecs-application-application-config-fixedoverload" aria-label="Permalink to &quot;tecs.application.Application.Config.fixedOverload&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.fixedOverload">tecs.application.Application.Config.fixedOverload</a>: ecs.FixedOverload
</code></pre><p>What becomes of the catch-up that did not fit. Defaults to &quot;drop&quot;, which bounds a frame&#39;s work and reports the loss through <code>world:getStats</code>. &quot;accumulate&quot; keeps every second instead. <a id="tecs.application.Application.Config.maxEntities"></a></p><h3 id="tecs-application-application-config-maxentities" tabindex="-1">tecs.application.Application.Config.maxEntities <a class="header-anchor" href="#tecs-application-application-config-maxentities" aria-label="Permalink to &quot;tecs.application.Application.Config.maxEntities&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.maxEntities">tecs.application.Application.Config.maxEntities</a>: integer
</code></pre><p>Entity slots the world is sized for. Defaults to 2^20, and 2^22 - 1 is the ceiling the packed id format allows.</p><p>Concurrent slots rather than lifetime spawns: a slot is given back when the entity is despawned and reused by the next one. The arena is preallocated for this many, so it is a memory decision as much as a limit, and a game that knows its population sets it rather than paying for a million slots it will not use. <a id="tecs.application.Application.Config.reserveRuns"></a></p><h3 id="tecs-application-application-config-reserveruns" tabindex="-1">tecs.application.Application.Config.reserveRuns <a class="header-anchor" href="#tecs-application-application-config-reserveruns" aria-label="Permalink to &quot;tecs.application.Application.Config.reserveRuns&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.reserveRuns">tecs.application.Application.Config.reserveRuns</a>: boolean
</code></pre><p>Give each archetype a run with room to grow rather than packing the runs end to end. Defaults to false, which packs.</p><p>Packed, a run cannot grow without shifting every run after it, so a spawn anywhere rewrites every instance in the scene. Reserved, it grows into its own slack and the archetypes around it are left alone. What it costs is that the cull is dispatched over the slack as well as over the rows, and that reserving needs headroom: a <code>capacity</code> sized to exactly the population leaves nothing to reserve out of and the setting does nothing.</p><p>Worth it for a scene that spawns into one archetype while most of the world sits in others, which is what a game with a level and projectiles in it looks like. Worth nothing for a scene held in a single archetype, where there is no other run to leave alone.</p><p>Not named for debugging, unlike <code>debugMaxFrames</code> below, because it is not a debugging setting: it is a tuning one, measured at 15.4x on a mixed-archetype spawner scene. A name with <code>debug</code> in it would warn off exactly the shipped games that should turn it on. <a id="tecs.application.Application.Config.packImages"></a></p><h3 id="tecs-application-application-config-packimages" tabindex="-1">tecs.application.Application.Config.packImages <a class="header-anchor" href="#tecs-application-application-config-packimages" aria-label="Permalink to &quot;tecs.application.Application.Config.packImages&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.packImages">tecs.application.Application.Config.packImages</a>: boolean
</code></pre><p>Fit many images into each layer of the renderer&#39;s image array rather than one. Defaults to false, where the ceiling on distinct images is the array&#39;s layer count and a small image costs a whole cell. On, the ceiling is the array&#39;s area instead. <a id="tecs.application.Application.Config.shadows"></a></p><h3 id="tecs-application-application-config-shadows" tabindex="-1">tecs.application.Application.Config.shadows <a class="header-anchor" href="#tecs-application-application-config-shadows" aria-label="Permalink to &quot;tecs.application.Application.Config.shadows&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.shadows">tecs.application.Application.Config.shadows</a>: Deferred.ShadowOptions
</code></pre><p>Let entities cast shadows, and tune what they cost. Nil, the default, means an <code>Occluder</code> or a <code>DropShadow</code> on an entity draws the entity and casts nothing, because the targets that would hold a shadow are never built. An empty table turns them on with every default; see <code>Deferred.ShadowOptions</code> for the seven numbers. <a id="tecs.application.Application.Config.debugMaxFrames"></a></p><h3 id="tecs-application-application-config-debugmaxframes" tabindex="-1">tecs.application.Application.Config.debugMaxFrames <a class="header-anchor" href="#tecs-application-application-config-debugmaxframes" aria-label="Permalink to &quot;tecs.application.Application.Config.debugMaxFrames&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.Config.debugMaxFrames">tecs.application.Application.Config.debugMaxFrames</a>: integer
</code></pre><p>Stops after this many iterations. Lets an automated run drive a real window to completion without a human closing it. <a id="tecs.application.Application.Config.plugin"></a></p><h3 id="tecs-application-application-config-plugin" tabindex="-1">tecs.application.Application.Config.plugin <a class="header-anchor" href="#tecs-application-application-config-plugin" aria-label="Permalink to &quot;tecs.application.Application.Config.plugin&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.Config.plugin">tecs.application.Application.Config.plugin</a>(ecs.World, Application)
</code></pre><p>The game, as one function handed the world and this application.</p><p>Called at the end of initialisation, after every engine subsystem is installed and before the startup phases run, so it can register a <code>Startup</code> system and have it run this initialisation rather than the next one.</p><p>One entry point rather than a list, because composing plugins is something the world already does: <code>world:addPlugin</code> takes an <code>ecs.Plugin</code> and is how the engine installs its own, so a game with several calls it from in here and needs no second mechanism.</p><p>The world comes first because every plugin the world takes is <code>function(world)</code>, so this reads as that shape with one more thing and code moves between here and a delegated plugin without a silent argument swap.</p><h4 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>ecs.World</code></td><td></td><td></td></tr><tr><td><code>Application</code></td><td></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.audio"></a></p><h3 id="tecs-application-application-audio" tabindex="-1">tecs.application.Application.audio <a class="header-anchor" href="#tecs-application-application-audio" aria-label="Permalink to &quot;tecs.application.Application.audio&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.audio">tecs.application.Application.audio</a>: Audio
</code></pre><p><a id="tecs.application.Application.checkpointPath"></a></p><h3 id="tecs-application-application-checkpointpath" tabindex="-1">tecs.application.Application.checkpointPath <a class="header-anchor" href="#tecs-application-application-checkpointpath" aria-label="Permalink to &quot;tecs.application.Application.checkpointPath&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.checkpointPath">tecs.application.Application.checkpointPath</a>(self: Application): string
</code></pre><p>Where the checkpoint is written, or nil when none was configured.</p><h4 id="parameters-1" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-1" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.clearCrash"></a></p><h3 id="tecs-application-application-clearcrash" tabindex="-1">tecs.application.Application.clearCrash <a class="header-anchor" href="#tecs-application-application-clearcrash" aria-label="Permalink to &quot;tecs.application.Application.clearCrash&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.clearCrash">tecs.application.Application.clearCrash</a>(self: Application): boolean, string
</code></pre><p>Another frame to look at and a chance to reload; the world may be inconsistent, reload before trusting it.</p><p>That sentence is the whole contract, and the name is deliberately not <code>recover</code> or <code>retry</code>: this is a debugging affordance, not fault tolerance. What the guard put back are the engine&#39;s invariants, which are the ones it can name: the command buffer is resolved, the passes are ended, the world is applying mutations again. A game&#39;s invariants are not restored and cannot be, because a system that threw halfway through its query left some entities updated and some not, and nothing here knows which half or what either half meant. So the frame after this is a frame to read, and the way to get back to a world worth trusting is to reload the scene.</p><p>Two gates, and they are different kinds of thing.</p><p>The first is whether this build is a development one at all, which is <code>debug</code>, <code>mcpPort</code> or <code>watch</code> in the config. A watcher counts because it is the configuration that most wants this: a game running one is being edited, and reloading the file that threw is exactly the way back. A shipped build has none of the three, latches on the first crash and stays latched, which is the right shipped behaviour: there is nobody there to read the frame, and carrying on with a world that is provably inconsistent is how one bug becomes a corrupted save.</p><p>The second is severity, and it is narrower rather than a judgement about how bad the throw looked. Recovery reports whether it had to force-end a pass or cancel a frame instead of submitting it, and in either case the device is not in the state the engine intended; see <code>_recover</code>. Those do not resume, at any setting, because the next frame would be recorded against a device nobody can describe.</p><p>The callers are the debug server&#39;s <code>clear_crash</code> tool and the file watcher&#39;s reloads. Both mean the same thing: something about the run has changed, try it again.</p><p>Physics resumability is a separate and open question: a solver stepped out of is not obviously a solver that can be stepped again.</p><h4 id="parameters-2" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-2" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns-1" tabindex="-1">Returns <a class="header-anchor" href="#returns-1" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>boolean</code></td><td>Whether the loop resumed, and why it did not.</td></tr><tr><td><code>string</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.crashed"></a></p><h3 id="tecs-application-application-crashed" tabindex="-1">tecs.application.Application.crashed <a class="header-anchor" href="#tecs-application-application-crashed" aria-label="Permalink to &quot;tecs.application.Application.crashed&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.crashed">tecs.application.Application.crashed</a>(self: Application): string
</code></pre><p>The gameplay traceback, or nil while the game is healthy.</p><h4 id="parameters-3" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-3" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns-2" tabindex="-1">Returns <a class="header-anchor" href="#returns-2" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.create"></a></p><h3 id="tecs-application-application-create" tabindex="-1">tecs.application.Application.create <a class="header-anchor" href="#tecs-application-application-create" aria-label="Permalink to &quot;tecs.application.Application.create&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.create">tecs.application.Application.create</a>(config: Application.Config): Application
</code></pre><p>Builds an application. Return the result from the entry chunk.</p><h4 id="parameters-4" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-4" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application.Config</code></td><td><code>config</code></td><td></td></tr></tbody></table><h4 id="returns-3" tabindex="-1">Returns <a class="header-anchor" href="#returns-3" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.device"></a></p><h3 id="tecs-application-application-device" tabindex="-1">tecs.application.Application.device <a class="header-anchor" href="#tecs-application-application-device" aria-label="Permalink to &quot;tecs.application.Application.device&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.device">tecs.application.Application.device</a>: Device
</code></pre><p><a id="tecs.application.Application.elapsed"></a></p><h3 id="tecs-application-application-elapsed" tabindex="-1">tecs.application.Application.elapsed <a class="header-anchor" href="#tecs-application-application-elapsed" aria-label="Permalink to &quot;tecs.application.Application.elapsed&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.elapsed">tecs.application.Application.elapsed</a>: number
</code></pre><p>Seconds of simulated time, advanced by the frame dt so a replay reproduces it exactly. <a id="tecs.application.Application.frame"></a></p><h3 id="tecs-application-application-frame" tabindex="-1">tecs.application.Application.frame <a class="header-anchor" href="#tecs-application-application-frame" aria-label="Permalink to &quot;tecs.application.Application.frame&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.frame">tecs.application.Application.frame</a>: integer
</code></pre><p>Iterations completed. <a id="tecs.application.Application.input"></a></p><h3 id="tecs-application-application-input" tabindex="-1">tecs.application.Application.input <a class="header-anchor" href="#tecs-application-application-input" aria-label="Permalink to &quot;tecs.application.Application.input&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.input">tecs.application.Application.input</a>: Input
</code></pre><p><a id="tecs.application.Application.mcp"></a></p><h3 id="tecs-application-application-mcp" tabindex="-1">tecs.application.Application.mcp <a class="header-anchor" href="#tecs-application-application-mcp" aria-label="Permalink to &quot;tecs.application.Application.mcp&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.mcp">tecs.application.Application.mcp</a>: mcp.Server
</code></pre><p>The debug server, when one was asked for. <a id="tecs.application.Application.quitRequested"></a></p><h3 id="tecs-application-application-quitrequested" tabindex="-1">tecs.application.Application.quitRequested <a class="header-anchor" href="#tecs-application-application-quitrequested" aria-label="Permalink to &quot;tecs.application.Application.quitRequested&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.quitRequested">tecs.application.Application.quitRequested</a>: boolean
</code></pre><p>Set to true to leave the loop at the end of the current iteration. <a id="tecs.application.Application.readCheckpoint"></a></p><h3 id="tecs-application-application-readcheckpoint" tabindex="-1">tecs.application.Application.readCheckpoint <a class="header-anchor" href="#tecs-application-application-readcheckpoint" aria-label="Permalink to &quot;tecs.application.Application.readCheckpoint&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.readCheckpoint">tecs.application.Application.readCheckpoint</a>(self: Application): string
</code></pre><p>The bytes the last run left, or nil when there are none.</p><p>Read it while building the world, which is what the plugin and the startup phases are for. Nil covers every reason there is nothing to resume from: a first run, a game that never staged anything, a file the player deleted. None of those is an error, so none of them raises.</p><h4 id="parameters-5" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-5" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns-4" tabindex="-1">Returns <a class="header-anchor" href="#returns-4" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.renderer"></a></p><h3 id="tecs-application-application-renderer" tabindex="-1">tecs.application.Application.renderer <a class="header-anchor" href="#tecs-application-application-renderer" aria-label="Permalink to &quot;tecs.application.Application.renderer&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.renderer">tecs.application.Application.renderer</a>: Renderer
</code></pre><p><a id="tecs.application.Application.stageCheckpoint"></a></p><h3 id="tecs-application-application-stagecheckpoint" tabindex="-1">tecs.application.Application.stageCheckpoint <a class="header-anchor" href="#tecs-application-application-stagecheckpoint" aria-label="Permalink to &quot;tecs.application.Application.stageCheckpoint&quot;">​</a></h3><pre><code>function <a href="#tecs.application.Application.stageCheckpoint">tecs.application.Application.stageCheckpoint</a>(self: Application, bytes: string)
</code></pre><p>Hands the engine the bytes to write when the platform backgrounds us.</p><p>Call this from an ordinary system, on an ordinary frame, whenever the state worth keeping has changed. Nothing is written here; the bytes are held, and <code>_willEnterBackground</code> writes them at the one moment the platform gives.</p><p>What this takes is bytes rather than a function that produces them, and that is the whole design rather than an inconvenience. iOS allows roughly five seconds from the backgrounding callback returning and Android rather less, and at this project&#39;s scale a world is not serialisable inside any of it: a callback that walked four million entities would be killed part way through and leave nothing behind. A function would let a game postpone the serialising into exactly that callback while looking like it had prepared something. A string cannot: by the time it exists, the expensive half has already happened on a frame that could afford it. The host times the hook and says so past 250 ms, which is the check on the rest.</p><p>Staging again replaces what was staged; there is one checkpoint, not a queue. Staging and then backgrounding twice writes once, because a write nothing has changed since is a second chance to be interrupted for nothing.</p><p>The counterpart is <code>readCheckpoint</code>, which is how the next run gets it back. Raises when no <code>checkpoint</code> was configured, because silently holding bytes that will never be written is the one failure a game would not notice.</p><h4 id="parameters-6" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-6" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>Application</code></td><td><code>self</code></td><td></td></tr><tr><td><code>string</code></td><td><code>bytes</code></td><td></td></tr></tbody></table><p><a id="tecs.application.Application.suspended"></a></p><h3 id="tecs-application-application-suspended" tabindex="-1">tecs.application.Application.suspended <a class="header-anchor" href="#tecs-application-application-suspended" aria-label="Permalink to &quot;tecs.application.Application.suspended&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.suspended">tecs.application.Application.suspended</a>: boolean
</code></pre><p>True while the platform has the application in the background. The loop still runs, but simulation and rendering do not. <a id="tecs.application.Application.window"></a></p><h3 id="tecs-application-application-window" tabindex="-1">tecs.application.Application.window <a class="header-anchor" href="#tecs-application-application-window" aria-label="Permalink to &quot;tecs.application.Application.window&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.window">tecs.application.Application.window</a>: Window
</code></pre><p><a id="tecs.application.Application.world"></a></p><h3 id="tecs-application-application-world" tabindex="-1">tecs.application.Application.world <a class="header-anchor" href="#tecs-application-application-world" aria-label="Permalink to &quot;tecs.application.Application.world&quot;">​</a></h3><pre><code><a href="#tecs.application.Application.world">tecs.application.Application.world</a>: ecs.World
</code></pre>`,238)])])}const u=t(n,[["render",s]]);export{g as __pageData,u as default};
