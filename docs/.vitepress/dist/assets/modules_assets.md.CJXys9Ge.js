import{_ as t,o as a,c as s,ag as i}from"./chunks/framework.BPmc_f4o.js";const k=JSON.parse('{"title":"tecs.assets","description":"Asynchronous image and sound loading on a worker thread, with handles, batches and explicit release","frontmatter":{"description":"Asynchronous image and sound loading on a worker thread, with handles, batches and explicit release","outline":"deep"},"headers":[],"relativePath":"modules/assets.md","filePath":"modules/assets.md"}'),h={name:"modules/assets.md"};function n(d,e,o,l,r,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /modules/assets.md for this page in Markdown format</div><h1 id="tecs-assets" tabindex="-1">tecs.assets <a class="header-anchor" href="#tecs-assets" aria-label="Permalink to &quot;tecs.assets&quot;">​</a></h1><p><code>tecs.assets</code> decodes images and sounds off the main thread. Decoding a PNG is milliseconds of pure CPU work with no GPU involvement, so it happens on a worker and the main thread only uploads. A load never blocks a frame: it hands back a handle immediately, the handle reports that it is still loading, and callers draw something else until it is not. Sound takes the same route for the same reason, because reading a file is a syscall a frame should not wait on.</p><p>Nothing here creates a GPU resource. Decoding and residency are separate decisions: the renderer knows what layout its textures need, and an asset that has been decoded is useful before anything has decided where it will live. Turning a decoded image into something drawable is <a href="/modules/gfx/"><code>Renderer</code></a>; turning a decoded clip into something audible is <a href="/modules/audio.html"><code>Audio</code></a>.</p><div class="info custom-block"><p class="custom-block-title">An image is a PNG or a JPEG</p><p>SDL_image offers eighteen formats and the build turns off every other one, because each is a codec a shipped binary carries whether or not it loads one. An atlas in any other format is converted at build time, not enabled at configure time. Sound is the other way round: whatever the mixer&#39;s decoders can read loads, and <code>Audio.decoders()</code> reports what the mixer actually linked.</p></div><h2 id="the-worker" tabindex="-1">The worker <a class="header-anchor" href="#the-worker" aria-label="Permalink to &quot;The worker&quot;">​</a></h2><h3 id="install" tabindex="-1">install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;install&quot;">​</a></h3><p>Starts the loading worker.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">install</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(luaPath?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>luaPath</code>: <code>package.path</code> for the worker&#39;s state. Defaults to this state&#39;s.</li></ul><p>Installing twice is installing once. Spawning unconditionally would leave the first thread running with both its channels and nothing reading them, and every decode already queued would answer into a channel that has been dropped, so a load in flight across the second call would never resolve.</p><p><a href="/modules/application.html"><code>Application</code></a> owns this: it installs the worker, calls <code>update</code> once per iteration and <code>shutdown</code> at teardown, so a game that loads an image and does nothing else still sees its handle resolve and the decoding thread still stops. A headless tool or a test that loads assets installs it itself.</p><h3 id="installed" tabindex="-1">installed <a class="header-anchor" href="#installed" aria-label="Permalink to &quot;installed&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">installed</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span></code></pre></div><p><strong>Returns:</strong> whether the loading worker is running. For a subsystem that loads an asset of its own and has no way of knowing whether the game has started the worker yet.</p><h3 id="update" tabindex="-1">update <a class="header-anchor" href="#update" aria-label="Permalink to &quot;update&quot;">​</a></h3><p>Takes finished decodes and resolves their handles. Call once per frame.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">update</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><p><strong>Returns:</strong> how many decodes were completed by this call. Zero when no worker is running.</p><h3 id="pending" tabindex="-1">pending <a class="header-anchor" href="#pending" aria-label="Permalink to &quot;pending&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">pending</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><p><strong>Returns:</strong> how many handles are still waiting on the worker.</p><h3 id="waitall" tabindex="-1">waitAll <a class="header-anchor" href="#waitall" aria-label="Permalink to &quot;waitAll&quot;">​</a></h3><p>Blocks until every queued load has finished.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">waitAll</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(timeoutMs?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">number</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>timeoutMs</code>: milliseconds to spend. Defaults to 5000.</li></ul><p>For startup and tests. A frame polls <code>update</code> instead. The budget is wall clock rather than a count of pumps, because a pump returns as soon as one message arrives.</p><h3 id="shutdown" tabindex="-1">shutdown <a class="header-anchor" href="#shutdown" aria-label="Permalink to &quot;shutdown&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">shutdown</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><p>Stops the loading worker and drops what was queued. Loading after this needs another <code>install</code>.</p><h2 id="loading" tabindex="-1">Loading <a class="header-anchor" href="#loading" aria-label="Permalink to &quot;Loading&quot;">​</a></h2><h3 id="loadimage" tabindex="-1">loadImage <a class="header-anchor" href="#loadimage" aria-label="Permalink to &quot;loadImage&quot;">​</a></h3><p>Queues an image for loading and returns its handle immediately.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">loadImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handle</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>path</code>: an absolute path, or one <a href="/modules/filesystem/"><code>filesystem</code></a> has resolved against the content root.</li></ul><p><strong>Returns:</strong> a <code>Handle</code> in the <code>&quot;loading&quot;</code> state. Calling this before <code>install</code> raises.</p><p>Two loads of one path that overlap share a decode and get the same handle, because decoding the same PNG twice at once is work with no result the first decode does not already have. They share the future with it, so the last of them to release is the one that frees. A load that starts after the first has resolved decodes again: nothing here is a cache, and a handle that has been released is not something to hand out a second time.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> handle </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">loadImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">filesystem</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">assetPath</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">sprites/hero.png</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">))</span></span>
<span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">-- ... frames later, once handle.status == &quot;ready&quot;</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> sprite </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> renderer:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">registerImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(handle)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">handle:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">release</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><h3 id="loadsound" tabindex="-1">loadSound <a class="header-anchor" href="#loadsound" aria-label="Permalink to &quot;loadSound&quot;">​</a></h3><p>Queues a sound for loading and returns its handle immediately.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">loadSound</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, mode: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, streamMs: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handle</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>path</code>: the file to read. Whatever the mixer&#39;s decoders can read loads, so the format is the file&#39;s business rather than the caller&#39;s.</li><li><code>mode</code>: <code>&quot;resident&quot;</code>, <code>&quot;stream&quot;</code> or <code>&quot;auto&quot;</code>.</li><li><code>streamMs</code>: the threshold <code>&quot;auto&quot;</code> decides on.</li></ul><table tabindex="0"><thead><tr><th>Mode</th><th>Effect</th></tr></thead><tbody><tr><td><code>&quot;resident&quot;</code></td><td>Decoded once, up front, and held as PCM.</td></tr><tr><td><code>&quot;stream&quot;</code></td><td>Nothing held; each voice opens the file for itself.</td></tr><tr><td><code>&quot;auto&quot;</code></td><td>Resident under <code>streamMs</code>, streamed at or over it.</td></tr></tbody></table><p>The auto case reads the file twice when it turns out to be short: once without decoding, to ask how long it is, and once to decode it. That is a second read of a small file, off the main thread, against holding a decoded copy of a piece of music.</p><p>A handle whose mixer could not be initialised comes back already <code>&quot;failed&quot;</code> rather than raising. <a href="/modules/audio.html"><code>Audio</code></a> is the normal caller; a game loads clips through it rather than through here.</p><h2 id="handle" tabindex="-1">Handle <a class="header-anchor" href="#handle" aria-label="Permalink to &quot;Handle&quot;">​</a></h2><p>What a load hands back. It is a one-shot future, not a cache: it settles once and release is terminal.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>kind</code></td><td><code>string</code></td><td>What was asked for: <code>&quot;image&quot;</code> or <code>&quot;sound&quot;</code>.</td></tr><tr><td><code>status</code></td><td><code>string</code></td><td><code>&quot;loading&quot;</code>, <code>&quot;ready&quot;</code>, <code>&quot;failed&quot;</code> or <code>&quot;released&quot;</code>.</td></tr><tr><td><code>path</code></td><td><code>string</code></td><td>The path this load was for.</td></tr><tr><td><code>pixels</code></td><td>cdata</td><td>Decoded RGBA pixels, valid until <code>release</code>. Images only.</td></tr><tr><td><code>width</code></td><td><code>integer</code></td><td>Image width in pixels.</td></tr><tr><td><code>height</code></td><td><code>integer</code></td><td>Image height in pixels.</td></tr><tr><td><code>pitch</code></td><td><code>integer</code></td><td>Row stride in bytes, which a decoder may pad beyond <code>width * 4</code>.</td></tr><tr><td><code>audio</code></td><td>cdata</td><td>The loaded clip, valid until <code>release</code>. Sounds only, and nil for one that streams, which holds nothing.</td></tr><tr><td><code>resident</code></td><td><code>boolean</code></td><td>Whether the clip is held in memory. Sounds only.</td></tr><tr><td><code>durationMs</code></td><td><code>integer</code></td><td>Length in milliseconds, or <code>-1</code> when the file cannot say. Sounds only.</td></tr><tr><td><code>error</code></td><td><code>string</code></td><td>Set if loading failed.</td></tr></tbody></table><p>Images are decoded to one normalised RGBA layout on the worker, so the upload path handles exactly one layout and the conversion cost lands off the main thread with the decode.</p><p><code>&quot;released&quot;</code> is terminal and reached only through <code>release</code>, so something handed a handle whose decode it no longer owns is told that rather than finding nil where the pixels were. That is the difference between a clear error at the upload and a null dereference inside it.</p><h3 id="release" tabindex="-1">release <a class="header-anchor" href="#release" aria-label="Permalink to &quot;release&quot;">​</a></h3><p>Frees what was decoded.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Handle</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">release</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><p>Called once whatever needed it has taken a copy, which for an image means after it has been uploaded. A voice reads a clip where it lies, so a clip is released when nothing will play it again; a track holds its own reference, so releasing one that is still sounding is safe and the mixer drops it when the last track using it does.</p><p>Releasing a load still in flight is allowed: what the worker returns is destroyed on arrival. Where several loads shared one decode this releases one of them, and the last one frees. Releasing twice does nothing.</p><h2 id="batch" tabindex="-1">Batch <a class="header-anchor" href="#batch" aria-label="Permalink to &quot;Batch&quot;">​</a></h2><p>A set of loads to wait on, with something of the caller&#39;s beside each.</p><p>Waiting on several handles at once is not one subsystem&#39;s problem. Sound holds a clip per handle, text holds an atlas per handle, and a sidecar holds whatever it was read for; each of them wants the same three things, which are how many are still in flight, a callback per one that finished, and a blocking form for startup and tests.</p><h3 id="batch-1" tabindex="-1">batch <a class="header-anchor" href="#batch-1" aria-label="Permalink to &quot;batch&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">&lt;</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">T</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">&gt;(finished: </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(key: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">T</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, handle: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handle</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">&lt;</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">T</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">&gt;</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>finished</code>: called once per load that leaves the <code>&quot;loading&quot;</code> state, with the caller&#39;s own value beside it. Required; passing nil raises.</li></ul><p><code>T</code> is the caller&#39;s and nothing here reads it. The callback is given once rather than at every drain, because what a batch does with a load is a property of who made it and not of the moment it is looked at.</p><h3 id="batch-methods" tabindex="-1">Batch methods <a class="header-anchor" href="#batch-methods" aria-label="Permalink to &quot;Batch methods&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">add</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(handle: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handle</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, key: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">T</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">pending</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">resolve</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">wait</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(timeoutMs?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">number</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><ul><li><code>add</code> adds a load to wait on, with the caller&#39;s value beside it.</li><li><code>pending</code> answers how many of its loads are still in flight.</li><li><code>resolve</code> drains the worker itself, hands every finished load to the callback, drops it, and returns how many were resolved. Taking whatever the worker has answered first is what lets a subsystem polling only its own batch still see its loads finish.</li><li><code>wait</code> blocks until nothing is in flight, then resolves. <code>timeoutMs</code> defaults to 5000. For startup, shutdown and tests; a frame calls <code>resolve</code> instead.</li></ul><div class="warning custom-block"><p class="custom-block-title">A callback must not add to the batch it is being called from</p><p>Resolving compacts the batch in place, so a frame with loads in flight allocates nothing. The price of that is that the compaction is walking the list the callback would be appending to.</p></div><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> loads </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">batch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(name: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, handle: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">tecs</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handle</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    if</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> handle.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">status</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> ==</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;"> &quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">ready</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;"> then</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">        atlases[name] </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> renderer:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">registerImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(handle)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    end</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">    handle:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">release</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">loads:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">add</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">loadImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">filesystem</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">assetPath</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">ui.png</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)), </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">ui</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">loads:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">add</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">assets</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">loadImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">filesystem</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">assetPath</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">tiles.png</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)), </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">tiles</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">-- once per frame</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">loads:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">resolve</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><h2 id="reading-a-document" tabindex="-1">Reading a document <a class="header-anchor" href="#reading-a-document" aria-label="Permalink to &quot;Reading a document&quot;">​</a></h2><p>Bytes a game interprets itself do not go through a decode at all. That is <a href="/modules/filesystem/"><code>filesystem</code></a>, which reads through the platform rather than through stdio, because on Android content lives inside the package and <code>io.open</code> does not reach it. It answers bytes and never text: a Lua string carries a length, so an image, an archive or a binary sidecar comes back whole, and a format that is text is a decoder&#39;s opinion.</p><p>Reading a document is therefore two halves with nothing between them:</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> bytes </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">filesystem</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">read</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">filesystem</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">assetPath</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">levels/1.json</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">))</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> level </span><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">&lt;const&gt;</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">data</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">decodeJSON</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(bytes)</span></span></code></pre></div><p><code>assetPath</code> resolves against the content root and <code>read</code> answers nil for a path with no file, so a game distinguishes an absent document from a malformed one, which the decoder raises on. Every path this process has read or decoded is recorded, including the ones queued here, and <code>filesystem.loaded</code> is what the file watcher polls instead of walking the content tree.</p><h2 id="what-a-load-hands-back-and-what-it-does-not" tabindex="-1">What a load hands back, and what it does not <a class="header-anchor" href="#what-a-load-hands-back-and-what-it-does-not" aria-label="Permalink to &quot;What a load hands back, and what it does not&quot;">​</a></h2><p>A <code>Handle</code> settles once and is read as a field, and that is deliberately the whole of it. The general settle-once value with combinators over it, which a child process or a request returns, is <a href="/modules/future.html"><code>Future</code></a>; it carries <code>status</code>, <code>value</code> and <code>error</code>, chains with <code>map</code>, <code>flatMap</code> and <code>recover</code>, and is what the sequencer&#39;s awaitable bridge parks a program on. A load queued here is not one of those: it is a handle, and the sequencer waits on it through whatever subsystem owns the decode.</p><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><p>Every function and type this module carries, rendered from <code>src/tecs/assets.tl</code>.</p><p><a id="tecs.assets.Batch"></a></p><h3 id="tecs-assets-batch" tabindex="-1">tecs.assets.Batch <a class="header-anchor" href="#tecs-assets-batch" aria-label="Permalink to &quot;tecs.assets.Batch&quot;">​</a></h3><pre><code>record <a href="#tecs.assets.Batch">tecs.assets.Batch</a>&lt;T&gt;
</code></pre><p>A set of loads to wait on, with something of the caller&#39;s beside each.</p><p>Waiting on several handles at once is not one subsystem&#39;s problem. Sound holds a clip per handle, text holds an atlas per handle, and a sidecar holds whatever it was read for; each of them wants the same three things, which are how many are still in flight, a callback per one that finished, and a blocking form for startup and tests. That is the whole shape, so it is here once rather than written again beside every list of handles.</p><p><code>T</code> is the caller&#39;s and nothing here reads it. Resolving compacts in place, so a frame with loads in flight allocates nothing; the price of that is that a callback must not add to the batch it is being called from.</p><h4 id="type-parameters" tabindex="-1">Type Parameters <a class="header-anchor" href="#type-parameters" aria-label="Permalink to &quot;Type Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Name</th><th>Constraint</th><th>Description</th></tr></thead><tbody><tr><td><code>T</code></td><td></td><td></td></tr></tbody></table><p><a id="tecs.assets.Batch.add"></a></p><h3 id="tecs-assets-batch-add" tabindex="-1">tecs.assets.Batch.add <a class="header-anchor" href="#tecs-assets-batch-add" aria-label="Permalink to &quot;tecs.assets.Batch.add&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.Batch.add">tecs.assets.Batch.add</a>(self: <a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;, handle: <a href="#tecs.assets.Handle">Handle</a>, key: T)
</code></pre><p>Adds a load to wait on, with the caller&#39;s value beside it.</p><p>Adding one handle twice waits on it twice and calls back twice. Adding one already finished is allowed; the next <code>resolve</code> hands it over.</p><h4 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;</code></td><td><code>self</code></td><td></td></tr><tr><td><code><a href="#tecs.assets.Handle">Handle</a></code></td><td><code>handle</code></td><td>Held rather than copied, so the batch sees whatever the worker settles it to. Nothing is started here: the load was already in flight before it arrived.</td></tr><tr><td><code>T</code></td><td><code>key</code></td><td>The caller&#39;s own value, handed back beside the handle at <code>resolve</code> so the callback knows which load it is looking at. Not read or compared by the batch, so duplicates are fine.</td></tr></tbody></table><p><a id="tecs.assets.Batch.pending"></a></p><h3 id="tecs-assets-batch-pending" tabindex="-1">tecs.assets.Batch.pending <a class="header-anchor" href="#tecs-assets-batch-pending" aria-label="Permalink to &quot;tecs.assets.Batch.pending&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.Batch.pending">tecs.assets.Batch.pending</a>(self: <a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;): integer
</code></pre><p>Loads still in flight.</p><h4 id="parameters-1" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-1" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>How many of the batch&#39;s handles read &quot;loading&quot; right now. Counts what is still unresolved, so it drops to zero a <code>resolve</code> before the batch empties.</td></tr></tbody></table><p><a id="tecs.assets.Batch.resolve"></a></p><h3 id="tecs-assets-batch-resolve" tabindex="-1">tecs.assets.Batch.resolve <a class="header-anchor" href="#tecs-assets-batch-resolve" aria-label="Permalink to &quot;tecs.assets.Batch.resolve&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.Batch.resolve">tecs.assets.Batch.resolve</a>(self: <a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;): integer
</code></pre><p>Hands every finished load to the callback and drops it.</p><p>Takes whatever the worker has answered first, so a subsystem polling only its own batch still sees its loads finish.</p><p>The callback gets a failed load as well as a ready one; check <code>handle.status</code> rather than assuming pixels. It must not add to this batch, which is being compacted underneath it.</p><h4 id="parameters-2" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-2" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;</code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns-1" tabindex="-1">Returns <a class="header-anchor" href="#returns-1" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>How many were resolved by this call.</td></tr></tbody></table><p><a id="tecs.assets.Batch.wait"></a></p><h3 id="tecs-assets-batch-wait" tabindex="-1">tecs.assets.Batch.wait <a class="header-anchor" href="#tecs-assets-batch-wait" aria-label="Permalink to &quot;tecs.assets.Batch.wait&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.Batch.wait">tecs.assets.Batch.wait</a>(self: <a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;, timeoutMs: number): integer
</code></pre><p>Blocks until nothing is in flight, then resolves.</p><p>For startup, shutdown and tests. A frame calls <code>resolve</code> instead.</p><h4 id="parameters-3" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-3" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;</code></td><td><code>self</code></td><td></td></tr><tr><td><code>number</code></td><td><code>timeoutMs</code></td><td>Milliseconds to wait. Defaults to 5000.</td></tr></tbody></table><h4 id="returns-2" tabindex="-1">Returns <a class="header-anchor" href="#returns-2" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>How many were resolved. A timeout is not an error and is not reported: the count is simply short and the rest stay in the batch, so check <code>pending</code> after this rather than trusting the return.</td></tr></tbody></table><p><a id="tecs.assets.Handle"></a></p><h3 id="tecs-assets-handle" tabindex="-1">tecs.assets.Handle <a class="header-anchor" href="#tecs-assets-handle" aria-label="Permalink to &quot;tecs.assets.Handle&quot;">​</a></h3><pre><code><a href="#tecs.assets.Handle">tecs.assets.Handle</a>: Handle
</code></pre><p>One asynchronous load. <a id="tecs.assets.batch"></a></p><h3 id="tecs-assets-batch-1" tabindex="-1">tecs.assets.batch <a class="header-anchor" href="#tecs-assets-batch-1" aria-label="Permalink to &quot;tecs.assets.batch&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.batch">tecs.assets.batch</a>&lt;T&gt;(finished: function(T, <a href="#tecs.assets.Handle">Handle</a>)): <a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;
</code></pre><p>A batch that hands each finished load to <code>finished</code>.</p><p>The callback is given once rather than at every drain, because what a batch does with a load is a property of who made it and not of the moment it is looked at.</p><h4 id="type-parameters-1" tabindex="-1">Type Parameters <a class="header-anchor" href="#type-parameters-1" aria-label="Permalink to &quot;Type Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Name</th><th>Constraint</th><th>Description</th></tr></thead><tbody><tr><td><code>T</code></td><td></td><td></td></tr></tbody></table><h4 id="parameters-4" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-4" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>function(T, <a href="#tecs.assets.Handle">Handle</a>)</code></td><td><code>finished</code></td><td>Called once per load that leaves &quot;loading&quot;, ready or failed alike, on the thread that drains the batch. Required; nil raises.</td></tr></tbody></table><h4 id="returns-3" tabindex="-1">Returns <a class="header-anchor" href="#returns-3" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Batch">Batch</a>&lt;T&gt;</code></td><td>An empty batch. Nothing registers it, so a batch nobody resolves simply holds its handles.</td></tr></tbody></table><p><a id="tecs.assets.install"></a></p><h3 id="tecs-assets-install" tabindex="-1">tecs.assets.install <a class="header-anchor" href="#tecs-assets-install" aria-label="Permalink to &quot;tecs.assets.install&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.install">tecs.assets.install</a>(luaPath: string)
</code></pre><p>Starts the loading worker.</p><p>Installing twice is installing once. Spawning unconditionally would leave the first thread running with both its channels and nothing reading them, and every decode already queued would answer into a channel that has been dropped, so a load in flight across the second call would never resolve.</p><h4 id="parameters-5" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-5" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>luaPath</code></td><td><code>package.path</code> for the worker&#39;s own state, which shares no loaded modules with this one. Defaults to this state&#39;s, which is what makes the worker resolve the same modules the game does.</td></tr></tbody></table><p><a id="tecs.assets.installed"></a></p><h3 id="tecs-assets-installed" tabindex="-1">tecs.assets.installed <a class="header-anchor" href="#tecs-assets-installed" aria-label="Permalink to &quot;tecs.assets.installed&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.installed">tecs.assets.installed</a>(): boolean
</code></pre><p>Whether the loading worker is running.</p><p>For a subsystem that loads an asset of its own and has no way of knowing whether the game has started the worker yet.</p><h4 id="returns-4" tabindex="-1">Returns <a class="header-anchor" href="#returns-4" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>boolean</code></td><td>Whether the worker exists, which is a fact about the process rather than about any world. False means a load raises rather than queueing, so this is the guard rather than an optimisation.</td></tr></tbody></table><p><a id="tecs.assets.loadImage"></a></p><h3 id="tecs-assets-loadimage" tabindex="-1">tecs.assets.loadImage <a class="header-anchor" href="#tecs-assets-loadimage" aria-label="Permalink to &quot;tecs.assets.loadImage&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.loadImage">tecs.assets.loadImage</a>(path: string): <a href="#tecs.assets.Handle">Handle</a>
</code></pre><p>Queues an image for loading and returns its handle immediately.</p><p>Two loads of one path that overlap share a decode and get the same handle, because decoding the same PNG twice at once is work with no result the first decode does not already have. They share the surface with it, so the last of them to release is the one that frees. A load that starts after the first has resolved decodes again: nothing here is a cache, and a handle that has been released is not something to hand out a second time.</p><h4 id="parameters-6" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-6" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>path</code></td><td>PNG or JPEG; no other format is compiled in, and one that is not either fails the decode rather than being refused here.</td></tr></tbody></table><h4 id="returns-5" tabindex="-1">Returns <a class="header-anchor" href="#returns-5" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Handle">Handle</a></code></td><td>A handle at &quot;loading&quot;, never nil. Raises instead when <code>install</code> has not run. Failures reach it through <code>status</code>, not through this call: a missing file is a handle that turns &quot;failed&quot; once the worker has looked.</td></tr></tbody></table><p><a id="tecs.assets.loadSound"></a></p><h3 id="tecs-assets-loadsound" tabindex="-1">tecs.assets.loadSound <a class="header-anchor" href="#tecs-assets-loadsound" aria-label="Permalink to &quot;tecs.assets.loadSound&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.loadSound">tecs.assets.loadSound</a>(path: string, mode: string, streamMs: integer): <a href="#tecs.assets.Handle">Handle</a>
</code></pre><p>Queues a sound for loading and returns its handle immediately.</p><p>Whatever the mixer&#39;s decoders can read loads, so the format is the file&#39;s business rather than the caller&#39;s.</p><p><code>mode</code> is &quot;resident&quot;, &quot;stream&quot;, or &quot;auto&quot;, and auto keeps anything shorter than <code>streamMs</code> resident.</p><p>The library is initialised here rather than on the worker: <code>MIX_Init</code> is documented as not thread safe, and doing it before the task is sent puts it in order ahead of every decode without a lock.</p><p>Two overlapping loads of one path do not share, unlike images: each gets its own clip.</p><h4 id="parameters-7" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-7" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>path</code></td><td></td></tr><tr><td><code>string</code></td><td><code>mode</code></td><td>&quot;resident&quot;, &quot;stream&quot; or &quot;auto&quot;. Anything else is treated as &quot;stream&quot;.</td></tr><tr><td><code>integer</code></td><td><code>streamMs</code></td><td>The boundary &quot;auto&quot; decides on, in milliseconds. Read only under &quot;auto&quot;, and a file whose length the container cannot state streams whatever it says.</td></tr></tbody></table><h4 id="returns-6" tabindex="-1">Returns <a class="header-anchor" href="#returns-6" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.assets.Handle">Handle</a></code></td><td>A handle at &quot;loading&quot;, or one already at &quot;failed&quot; when the mixer could not be initialised, which is the one failure reported without waiting for the worker.</td></tr></tbody></table><p><a id="tecs.assets.pending"></a></p><h3 id="tecs-assets-pending" tabindex="-1">tecs.assets.pending <a class="header-anchor" href="#tecs-assets-pending" aria-label="Permalink to &quot;tecs.assets.pending&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.pending">tecs.assets.pending</a>(): integer
</code></pre><p>Handles still waiting on the worker.</p><h4 id="returns-7" tabindex="-1">Returns <a class="header-anchor" href="#returns-7" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>Every load in flight in this process, not one caller&#39;s. Counted by walking the set, so it is fine per frame and not per entity.</td></tr></tbody></table><p><a id="tecs.assets.shutdown"></a></p><h3 id="tecs-assets-shutdown" tabindex="-1">tecs.assets.shutdown <a class="header-anchor" href="#tecs-assets-shutdown" aria-label="Permalink to &quot;tecs.assets.shutdown&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.shutdown">tecs.assets.shutdown</a>()
</code></pre><p>Stops the loading worker.</p><p>Blocks until the thread has exited. Loads still in flight are dropped and their handles stay at &quot;loading&quot; forever, so drain with <code>waitAll</code> first if their results are wanted. Releasing a handle that is already ready still works afterwards; this frees nothing. <a id="tecs.assets.update"></a></p><h3 id="tecs-assets-update" tabindex="-1">tecs.assets.update <a class="header-anchor" href="#tecs-assets-update" aria-label="Permalink to &quot;tecs.assets.update&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.update">tecs.assets.update</a>(): integer
</code></pre><p>Takes finished decodes and uploads them. Call once per frame.</p><p>Polls, never blocks, and drains everything the worker has answered rather than one result. Handles do not change state without this, so a game that stops calling it has loads that stay &quot;loading&quot; forever.</p><h4 id="returns-8" tabindex="-1">Returns <a class="header-anchor" href="#returns-8" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>How many handles moved off &quot;loading&quot; this call, which is zero when nothing has finished and also zero when no worker is installed.</td></tr></tbody></table><p><a id="tecs.assets.waitAll"></a></p><h3 id="tecs-assets-waitall" tabindex="-1">tecs.assets.waitAll <a class="header-anchor" href="#tecs-assets-waitall" aria-label="Permalink to &quot;tecs.assets.waitAll&quot;">​</a></h3><pre><code>function <a href="#tecs.assets.waitAll">tecs.assets.waitAll</a>(timeoutMs: number)
</code></pre><p>Blocks until every queued load has finished.</p><p>For startup and tests. A frame should poll <code>update</code> instead.</p><h4 id="parameters-8" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-8" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>number</code></td><td><code>timeoutMs</code></td><td>Wall-clock milliseconds, defaulting to 5000. Running out is not an error and is not reported, so read <code>assets.pending</code> afterwards to tell the two endings apart.</td></tr></tbody></table>`,196)])])}const g=t(h,[["render",n]]);export{k as __pageData,g as default};
