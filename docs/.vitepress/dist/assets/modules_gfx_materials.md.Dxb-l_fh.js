import{_ as e,o as t,c as s,ag as i}from"./chunks/framework.BPmc_f4o.js";const m=JSON.parse('{"title":"tecs.gfx.materials","description":"How a material is authored, how a name becomes an id, and the module that reads materials and publishes the dispatch","frontmatter":{"description":"How a material is authored, how a name becomes an id, and the module that reads materials and publishes the dispatch","outline":"deep"},"headers":[],"relativePath":"modules/gfx/materials.md","filePath":"modules/gfx/materials.md"}'),n={name:"modules/gfx/materials.md"};function r(h,a,d,l,o,c){return t(),s("div",null,[...a[0]||(a[0]=[i(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /modules/gfx/materials.md for this page in Markdown format</div><h1 id="tecs-gfx-materials" tabindex="-1">tecs.gfx.materials <a class="header-anchor" href="#tecs-gfx-materials" aria-label="Permalink to &quot;tecs.gfx.materials&quot;">​</a></h1><p>A material is what a fragment is, decided per instance. It is a <code>.glsl</code> file under <code>materials/</code> implementing the contract in <code>shaders/include/material.glsl</code>. Every material found is compiled into the same fragment shader, its <code>material</code> function renamed, and a generated dispatch selects between them by an id the instance carries.</p><p>One shader rather than one per material, because the alternative is a pipeline and a draw call per material, and the whole point of the unified batch is that the scene is one draw. What it costs is fragment divergence where adjacent instances differ, which archetype-contiguous layout already keeps small: entities of a kind cluster.</p><p><code>tecs.gfx.materials</code> is the host half of that: it finds the files, assigns the ids, builds the dispatch and hands it to the shader loader.</p><h2 id="selecting-a-material-on-an-entity" tabindex="-1">Selecting a material on an entity <a class="header-anchor" href="#selecting-a-material-on-an-entity" aria-label="Permalink to &quot;Selecting a material on an entity&quot;">​</a></h2><p>An entity with no <code>Material</code> component draws as the default, which samples the image array and covers the whole quad, so an entity with neither a <code>Sprite</code> nor a <code>Material</code> still draws. Present, the component selects one of the materials found under <code>materials/</code>:</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">world:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">spawn</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span></span>
<span class="line"><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">    Transform</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(x, y),</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">    Renderable,</span></span>
<span class="line"><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">    Tint</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">0.2</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">0.6</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">1.0</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">1.0</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">),</span></span>
<span class="line"><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">    Material</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(materials.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">id</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">rounded</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">), </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">0.35</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p><code>Material</code> carries an <code>id</code> and a <code>param</code> from zero to one; see <a href="/modules/gfx/">components</a>. Resolve the id by name rather than writing a number: the numbering depends on which material files exist, and a literal would break the moment a material was added ahead of it alphabetically.</p><p>The parameter is one number because the instance packs the id and the parameter into a single float, the integer part selecting and the fraction carrying. So each material spends it on the one thing the transform cannot say, and a material that genuinely needed two would mean growing every instance in the scene.</p><h2 id="the-materials-the-engine-ships" tabindex="-1">The materials the engine ships <a class="header-anchor" href="#the-materials-the-engine-ships" aria-label="Permalink to &quot;The materials the engine ships&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Name</th><th>What it draws</th><th>What <code>param</code> means</th></tr></thead><tbody><tr><td><code>textured</code></td><td>The image&#39;s own silhouette</td><td>Nothing</td></tr><tr><td><code>circle</code></td><td>A circle inscribed in the quad</td><td>Nothing</td></tr><tr><td><code>ellipse</code></td><td>An ellipse filling the quad&#39;s width</td><td>Its height as a fraction of the quad&#39;s</td></tr><tr><td><code>ring</code></td><td>A circle with a hole</td><td>The hole&#39;s radius as a fraction of the outer one</td></tr><tr><td><code>rounded</code></td><td>A rectangle with rounded corners</td><td>The corner radius as a fraction of the quad</td></tr><tr><td><code>frame</code></td><td>The quad&#39;s outline</td><td>The border&#39;s thickness as a fraction of the quad&#39;s half extent</td></tr><tr><td><code>capsule</code></td><td>A bar with semicircular caps</td><td>Its thickness as a fraction of the quad&#39;s height</td></tr><tr><td><code>line</code></td><td>A thick line along the quad&#39;s diagonal</td><td>Its thickness as a fraction of the quad</td></tr><tr><td><code>pie</code></td><td>A wedge of a circle, opening upwards</td><td>The sweep as a fraction of a full turn</td></tr><tr><td><code>star</code></td><td>A five-pointed star, tip upwards</td><td>How deep the valleys between the tips cut</td></tr><tr><td><code>triangle</code></td><td>A triangle, apex upwards</td><td>Nothing</td></tr><tr><td><code>glyph</code></td><td>A multi-channel distance-field glyph</td><td>The field&#39;s range as a fraction of an atlas cell</td></tr></tbody></table><p><code>textured</code> takes its coverage from the texel&#39;s alpha at a threshold of a half, which is what makes a cut-out sprite a cut-out: the pass writes depth and does not blend, so a quad that covered its whole rectangle would hide whatever stood behind the transparent part of the image as well as painting over it. The test is on the texture&#39;s own alpha rather than on the product with the tint, so an entity with no <code>Sprite</code> samples the opaque white layer and draws at whatever tint alpha it carries. A half rather than any nonzero alpha because coverage here is a yes or a no: a texel kept at low alpha lands at full strength as a dark fringe rather than as a soft edge. An image authored with a soft edge therefore gets a hard one, cut where the artwork crosses half alpha.</p><p><code>glyph</code> is the material <a href="/modules/gfx/">text</a> selects, and it is unlit, so a caption draws at its own colour rather than being left in the dark by a scene&#39;s lights.</p><p>Only three of them claim a shape to the lighting pass: <code>circle</code> and <code>ellipse</code> return a dome, <code>capsule</code> returns a cylinder with hemispherical caps, and everything else is flat and facing the viewer. That is per material rather than per texture, so a sprite that wants a normal per texel wants a normal map, which is a sidecar image and is not built.</p><p><code>line</code> is the one whose parameterisation is worth stating: it runs along the quad&#39;s diagonal, so placing it at the midpoint of two points and scaling it by their signed difference draws the segment joining them. A negative scale mirrors the quad and takes the diagonal with it, so either direction works without the material knowing which.</p><h2 id="authoring-one" tabindex="-1">Authoring one <a class="header-anchor" href="#authoring-one" aria-label="Permalink to &quot;Authoring one&quot;">​</a></h2><p>A material file defines one function, <code>material</code>, taking a <code>MaterialInput</code> and answering a <code>MaterialOutput</code>. It decides what colour a fragment is and whether the fragment exists at all; it does not decide where the geometry is, which is the instance&#39;s transform and the same for every material.</p><p><strong>What it is handed:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>What it is</th></tr></thead><tbody><tr><td><code>local</code></td><td><code>vec2</code></td><td>Position within the quad, -0.5 to 0.5 on both axes</td></tr><tr><td><code>uv</code></td><td><code>vec3</code></td><td>Atlas coordinates and array layer, for a material that samples</td></tr><tr><td><code>color</code></td><td><code>vec4</code></td><td>The instance&#39;s tint</td></tr><tr><td><code>param</code></td><td><code>float</code></td><td>The instance&#39;s material parameter, zero to one</td></tr></tbody></table><p><strong>What it answers with:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>What it is</th></tr></thead><tbody><tr><td><code>albedo</code></td><td><code>vec4</code></td><td>The fragment&#39;s colour</td></tr><tr><td><code>normal</code></td><td><code>vec3</code></td><td>Which way the surface faces, in the quad&#39;s own space: X and Y along its axes and +Z out of it</td></tr><tr><td><code>lit</code></td><td><code>float</code></td><td>Zero leaves the fragment out of the lighting pass entirely, so it draws at its own colour</td></tr><tr><td><code>coverage</code></td><td><code>float</code></td><td>At or below zero the fragment is discarded</td></tr></tbody></table><p>Coverage rather than alpha, because the G-buffer pass writes with replace rather than blend: a partly covered fragment would overwrite what is behind it instead of blending into it. Each shape material answers with a signed distance rather than a yes or a no, positive inside, in the quad&#39;s own coordinates, so an edge is as exact at five hundred pixels as at five.</p><p>Every material starts from <code>materialDefaults()</code> rather than declaring a bare <code>MaterialOutput</code>, which is what lets the contract grow a field without every material in every root having to learn about it on the same day. The default normal is flat and facing the viewer, because a 2D sprite genuinely has no normal, and a material claims a shape only where its own silhouette is one.</p><p>A normal is carried into the world by the instance&#39;s rotation and by whichever axes its scale mirrored, and not by the scale itself, so writing a dome on an entity stretched to ten by one still gives a dome rather than something read as flat. A shape is taken to swell with itself: a 2D transform says how wide and how tall a thing is drawn and nothing about how far it stands out of the plane. A negative scale does flip the normal with the quad, which is what a mirrored surface facing the other way means.</p><p>Two helpers come with the contract: <code>domeNormal(rim)</code>, the normal of a dome over a flat field, and <code>sdRoundedBox(p, extent, radius)</code>, the signed distance to a rounded box. The shared image array is bound as <code>images</code>, sampled nearest.</p><div class="language-glsl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">glsl</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">// A rectangle with rounded corners. \`param\` is the radius, as a fraction of</span></span>
<span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">// the quad, so half of it is a circle.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">MaterialOutput </span><span style="--shiki-light:#622CBC;--shiki-dark:#7AA2F7;">material</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">(</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">MaterialInput </span><span style="--shiki-light:#702C00;--shiki-dark:#E0AF68;">frag</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">)</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;"> {</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">    MaterialOutput result </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#622CBC;--shiki-dark:#7AA2F7;"> materialDefaults</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">()</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">;</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    result</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">albedo</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#622CBC;--shiki-dark:#7AA2F7;"> texture</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">(</span><span style="--shiki-light:#0E1116;--shiki-dark:#7AA2F7;">images</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">,</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;"> frag</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">uv</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">) </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">*</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;"> frag</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">color</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">;</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    result</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">coverage</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> -</span><span style="--shiki-light:#622CBC;--shiki-dark:#7AA2F7;">sdRoundedBox</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">(</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">frag</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">local</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">,</span><span style="--shiki-light:#622CBC;--shiki-dark:#7AA2F7;"> vec2</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">(</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">0.5</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">)</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">,</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;"> frag</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">param</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">)</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">;</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    result</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">lit</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;"> 1.0</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">;</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    return</span><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;"> result</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">;</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#9ABDF5;">}</span></span></code></pre></div><p>The file&#39;s name without its extension is the material&#39;s name, and that is how a game asks for it. The function is renamed rather than namespaced on the way into the dispatch, because GLSL has no namespaces and two materials both defining <code>material</code> would collide.</p><h2 id="names-and-ids" tabindex="-1">Names and ids <a class="header-anchor" href="#names-and-ids" aria-label="Permalink to &quot;Names and ids&quot;">​</a></h2><p>The default material always takes id zero, because an entity with no <code>Material</code> component writes a zero and that has to mean something specific. The rest are numbered from one by sorted name, so the same files always produce the same numbering.</p><h3 id="defaultname" tabindex="-1">defaultName <a class="header-anchor" href="#defaultname" aria-label="Permalink to &quot;defaultName&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">materials.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">defaultName</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span></code></pre></div><p>Name of the material an entity with no <code>Material</code> component draws as. It is <code>textured</code>.</p><h3 id="id" tabindex="-1">id <a class="header-anchor" href="#id" aria-label="Permalink to &quot;id&quot;">​</a></h3><p>The id a material is dispatched by, resolved from its name.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">id</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(name: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><p><strong>Returns:</strong> the id. A name no material was found under raises, and the message lists what was found. Calling this installs the materials if they are not installed already.</p><h3 id="find" tabindex="-1">find <a class="header-anchor" href="#find" aria-label="Permalink to &quot;find&quot;">​</a></h3><p>The same lookup, answering nil instead of raising.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">find</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(name: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><p><strong>Returns:</strong> the id, or nil when nothing has that name. What <code>id</code> is built on, for a caller with somewhere better to put the refusal than an error raised from here.</p><h3 id="name" tabindex="-1">name <a class="header-anchor" href="#name" aria-label="Permalink to &quot;name&quot;">​</a></h3><p>The material an id dispatches to.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">name</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(id: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span></code></pre></div><p><strong>Returns:</strong> the name, or nil when nothing has that id.</p><h3 id="names" tabindex="-1">names <a class="header-anchor" href="#names" aria-label="Permalink to &quot;names&quot;">​</a></h3><p>Every material name, in id order.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">names</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span></code></pre></div><p><strong>Returns:</strong> a fresh list the caller owns. The default first at id zero, then the rest alphabetically. It is not sorted as a whole: the default&#39;s place is fixed by what a zero on an instance has to mean, wherever its own name would otherwise fall.</p><div class="warning custom-block"><p class="custom-block-title">Ids are not stable across a rebuild</p><p>Adding a material renumbers the ones after it alphabetically. That matters only for something that stored an id across a rebuild, which is why <code>Material</code> components are built from <code>materials.id(name)</code> and never from a literal.</p></div><p>That is also why a <code>Material</code> crosses a snapshot as its material&#39;s name rather than as its id, the way a <code>Sprite</code> carries its image&#39;s name. A snapshot outlives the process, and a build with one more material file would read an id that means a different shader and render the scene wrong without saying so. <code>name</code> writes it and <code>find</code> resolves it again; a name this build does not have raises and says which one, since falling back to the default or dropping the component are the same silent wrong shader by another route.</p><h2 id="adding-your-own" tabindex="-1">Adding your own <a class="header-anchor" href="#adding-your-own" aria-label="Permalink to &quot;Adding your own&quot;">​</a></h2><h3 id="addroot" tabindex="-1">addRoot <a class="header-anchor" href="#addroot" aria-label="Permalink to &quot;addRoot&quot;">​</a></h3><p>Adds a directory to search before the ones already known, so a game&#39;s materials are found alongside the engine&#39;s.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">addRoot</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p>The default root is <code>materials/</code> under the asset root. A nearer root wins a name, and roots added later are searched first. Adding a root puts the dispatch back to being unbuilt, so the next shader load rebuilds it.</p><h3 id="define" tabindex="-1">define <a class="header-anchor" href="#define" aria-label="Permalink to &quot;define&quot;">​</a></h3><p>Supplies a material from memory rather than a file.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">define</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(name: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, source: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p>For a spec, and for a game that generates one at build time. A definition beats a file of the same name in any root, and renumbers the set on the next <code>install</code> exactly as adding a file would.</p><h3 id="install" tabindex="-1">install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;install&quot;">​</a></h3><p>Reads the materials and publishes the dispatch. Idempotent.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">install</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><p>Called by shader loading rather than by a game, so a fragment shader cannot be built before the materials it dispatches to are known. Adding a root or defining a material puts this back on.</p><h3 id="reset" tabindex="-1">reset <a class="header-anchor" href="#reset" aria-label="Permalink to &quot;reset&quot;">​</a></h3><p>Forgets everything read, so a spec can start from the files again.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reset</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><h2 id="re-reading-materials" tabindex="-1">Re-reading materials <a class="header-anchor" href="#re-reading-materials" aria-label="Permalink to &quot;Re-reading materials&quot;">​</a></h2><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> materials</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reload</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span></code></pre></div><p><strong>Returns:</strong> whether the dispatch was rebuilt, then why not.</p><p>Re-reads every material in every root and republishes the dispatch. Materials supplied through <code>define</code> are kept as they are: those came from memory rather than a root, and nothing on disk has an opinion about them.</p><p>Refused when the set of materials has changed, and that refusal is the whole reason there is a rule here rather than a plain re-read. Ids come from sorted name order, so a file appearing or disappearing renumbers every material after it alphabetically, while a <code>Material</code> component in a live world holds a number that was resolved before the change. Editing a body reloads; adding or removing one is a restart. A refusal puts back exactly what was dispatched before, ids included.</p><p>The <a href="/modules/mcp.html">debug server</a> exposes this as its <code>reload_shaders</code> tool, together with the shader half of the reload. A build that links no compiler cannot reload at all, because a pack decided the shader format the device claimed when it was created.</p><h2 id="where-a-material-ends-up" tabindex="-1">Where a material ends up <a class="header-anchor" href="#where-a-material-ends-up" aria-label="Permalink to &quot;Where a material ends up&quot;">​</a></h2><p>The dispatch is published to the shader loader as a generated include named <code>materials.glsl</code>, which the instance fragment shader includes; it has no file of its own. GLSL goes to SPIR-V through shaderc, then to MSL through SPIRV-Cross.</p><p>A release compiles nothing. <code>make shaders</code> walks the shader registry and writes a pack, and the builder installs the materials before it enumerates anything, so the packaged dispatch knows about exactly the material files that were present at build time and cannot drift from what a development build would have compiled. <code>assets/materials/</code> is globbed when the tree is assembled, so a material file added there is in the build the next time it runs.</p><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><p>Every function and type this module carries, rendered from <code>src/tecs/gpu/materials.tl</code>.</p><p><a id="tecs.gfx.materials.Material"></a></p><h3 id="tecs-gfx-materials-material" tabindex="-1">tecs.gfx.materials.Material <a class="header-anchor" href="#tecs-gfx-materials-material" aria-label="Permalink to &quot;tecs.gfx.materials.Material&quot;">​</a></h3><pre><code>record <a href="#tecs.gfx.materials.Material">tecs.gfx.materials.Material</a>
</code></pre><p>One material: its name, the id this build gave it, and its source. <a id="tecs.gfx.materials.Material.name"></a></p><h3 id="tecs-gfx-materials-material-name" tabindex="-1">tecs.gfx.materials.Material.name <a class="header-anchor" href="#tecs-gfx-materials-material-name" aria-label="Permalink to &quot;tecs.gfx.materials.Material.name&quot;">​</a></h3><pre><code><a href="#tecs.gfx.materials.Material.name">tecs.gfx.materials.Material.name</a>: string
</code></pre><p>The file&#39;s name without its extension, and how a game asks for it. <a id="tecs.gfx.materials.Material.id"></a></p><h3 id="tecs-gfx-materials-material-id" tabindex="-1">tecs.gfx.materials.Material.id <a class="header-anchor" href="#tecs-gfx-materials-material-id" aria-label="Permalink to &quot;tecs.gfx.materials.Material.id&quot;">​</a></h3><pre><code><a href="#tecs.gfx.materials.Material.id">tecs.gfx.materials.Material.id</a>: integer
</code></pre><p>What an instance carries to select this material. Assigned by <code>install</code> from sorted order, so it holds only for the set of files that were present when the dispatch was built. <a id="tecs.gfx.materials.Material.source"></a></p><h3 id="tecs-gfx-materials-material-source" tabindex="-1">tecs.gfx.materials.Material.source <a class="header-anchor" href="#tecs-gfx-materials-material-source" aria-label="Permalink to &quot;tecs.gfx.materials.Material.source&quot;">​</a></h3><pre><code><a href="#tecs.gfx.materials.Material.source">tecs.gfx.materials.Material.source</a>: string
</code></pre><p>The material&#39;s GLSL, as read. Renamed on the way into the dispatch rather than here, so this is still the text the file holds. <a id="tecs.gfx.materials.addRoot"></a></p><h3 id="tecs-gfx-materials-addroot" tabindex="-1">tecs.gfx.materials.addRoot <a class="header-anchor" href="#tecs-gfx-materials-addroot" aria-label="Permalink to &quot;tecs.gfx.materials.addRoot&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.addRoot">tecs.gfx.materials.addRoot</a>(path: string)
</code></pre><p>Adds a directory to search before the ones already known, so a game&#39;s materials are found alongside the engine&#39;s.</p><h4 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>path</code></td><td>Searched ahead of every root already added, so a game&#39;s file of a given name beats the engine&#39;s. A trailing slash is added when it is missing. The directory is not read here: this only puts the next <code>install</code> back on.</td></tr></tbody></table><p><a id="tecs.gfx.materials.defaultName"></a></p><h3 id="tecs-gfx-materials-defaultname" tabindex="-1">tecs.gfx.materials.defaultName <a class="header-anchor" href="#tecs-gfx-materials-defaultname" aria-label="Permalink to &quot;tecs.gfx.materials.defaultName&quot;">​</a></h3><pre><code><a href="#tecs.gfx.materials.defaultName">tecs.gfx.materials.defaultName</a>: string
</code></pre><p>Name of the material an entity with no Material component draws as. <a id="tecs.gfx.materials.define"></a></p><h3 id="tecs-gfx-materials-define" tabindex="-1">tecs.gfx.materials.define <a class="header-anchor" href="#tecs-gfx-materials-define" aria-label="Permalink to &quot;tecs.gfx.materials.define&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.define">tecs.gfx.materials.define</a>(name: string, source: string)
</code></pre><p>Supplies a material from memory rather than a file.</p><p>For a spec, and for a game that generates one at build time. Beats a file of the same name in any root, and renumbers the set on the next <code>install</code> as adding a file would.</p><h4 id="parameters-1" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-1" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>name</code></td><td>The name a game asks for, and what a file would have been called without its extension.</td></tr><tr><td><code>string</code></td><td><code>source</code></td><td>The material&#39;s GLSL, taken as given and renamed only on the way into the dispatch.</td></tr></tbody></table><p><a id="tecs.gfx.materials.find"></a></p><h3 id="tecs-gfx-materials-find" tabindex="-1">tecs.gfx.materials.find <a class="header-anchor" href="#tecs-gfx-materials-find" aria-label="Permalink to &quot;tecs.gfx.materials.find&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.find">tecs.gfx.materials.find</a>(name: string): integer
</code></pre><p>The id a material is dispatched by, or nil when nothing has that name.</p><p>What <code>id</code> is built on, for a caller with somewhere better to put the refusal than an error raised from here. Reading a <code>Material</code> back out of a snapshot is that caller: the name it holds is a fact about the file rather than about the call site, and the message says so.</p><h4 id="parameters-2" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-2" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>name</code></td><td>The material&#39;s file name without its extension.</td></tr></tbody></table><h4 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>The id, or nil for a name nothing has. Installs first, so a name is resolved against the materials on disk rather than against whatever had been read so far.</td></tr></tbody></table><p><a id="tecs.gfx.materials.id"></a></p><h3 id="tecs-gfx-materials-id" tabindex="-1">tecs.gfx.materials.id <a class="header-anchor" href="#tecs-gfx-materials-id" aria-label="Permalink to &quot;tecs.gfx.materials.id&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.id">tecs.gfx.materials.id</a>(name: string): integer
</code></pre><p>The id a material is dispatched by, or an error naming what was found.</p><p>Resolved by name so a game never writes a number: the numbering depends on which files exist, and hard-coding one would break the moment a material was added ahead of it alphabetically.</p><h4 id="parameters-3" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-3" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>name</code></td><td>The material&#39;s file name without its extension.</td></tr></tbody></table><h4 id="returns-1" tabindex="-1">Returns <a class="header-anchor" href="#returns-1" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td>The id an instance carries to select it. Raises on a name nothing has, listing what there was, because a misspelled material is a build mistake rather than a runtime condition.</td></tr></tbody></table><p><a id="tecs.gfx.materials.install"></a></p><h3 id="tecs-gfx-materials-install" tabindex="-1">tecs.gfx.materials.install <a class="header-anchor" href="#tecs-gfx-materials-install" aria-label="Permalink to &quot;tecs.gfx.materials.install&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.install">tecs.gfx.materials.install</a>()
</code></pre><p>Reads the materials and publishes the dispatch. Idempotent.</p><p>Called by shader loading rather than by a game, so a fragment shader cannot be built before the materials it dispatches to are known. Adding a root or defining a material puts this back on, and the next load rebuilds. <a id="tecs.gfx.materials.name"></a></p><h3 id="tecs-gfx-materials-name" tabindex="-1">tecs.gfx.materials.name <a class="header-anchor" href="#tecs-gfx-materials-name" aria-label="Permalink to &quot;tecs.gfx.materials.name&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.name">tecs.gfx.materials.name</a>(id: integer): string
</code></pre><p>The material an id dispatches to, or nil when nothing has that id.</p><p>The reverse of <code>id</code>, and what a snapshot writes in place of the number. Nothing is kept in step to answer it: <code>order</code> already holds the names at their ids, counting from one, so this is the lookup the numbering was assigned from rather than a second copy of it.</p><h4 id="parameters-4" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-4" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td><code>id</code></td><td>An id as an instance carries it. Nil answers nil rather than raising, so a component read back with no material needs no guard.</td></tr></tbody></table><h4 id="returns-2" tabindex="-1">Returns <a class="header-anchor" href="#returns-2" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td>The material&#39;s name, or nil when nothing has that id.</td></tr></tbody></table><p><a id="tecs.gfx.materials.names"></a></p><h3 id="tecs-gfx-materials-names" tabindex="-1">tecs.gfx.materials.names <a class="header-anchor" href="#tecs-gfx-materials-names" aria-label="Permalink to &quot;tecs.gfx.materials.names&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.names">tecs.gfx.materials.names</a>(): {string}
</code></pre><p>Every material name, in id order.</p><p>The default first at id zero, then the rest alphabetically. Not sorted as a whole: the default&#39;s place is fixed by what a zero on an instance has to mean, wherever its own name would otherwise fall.</p><h4 id="returns-3" tabindex="-1">Returns <a class="header-anchor" href="#returns-3" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>{string}</code></td><td>A fresh list the caller owns, indexed from one while the ids it describes count from zero.</td></tr></tbody></table><p><a id="tecs.gfx.materials.reload"></a></p><h3 id="tecs-gfx-materials-reload" tabindex="-1">tecs.gfx.materials.reload <a class="header-anchor" href="#tecs-gfx-materials-reload" aria-label="Permalink to &quot;tecs.gfx.materials.reload&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.reload">tecs.gfx.materials.reload</a>(): boolean, string
</code></pre><p>Re-reads every material and republishes the dispatch.</p><p>Refused when the set of materials has changed, and this is the whole reason there is a rule rather than a re-read. Ids come from sorted name order, so a file appearing or disappearing renumbers every material after it alphabetically, and a <code>Material</code> component in a live world holds a number that was resolved before the change. Editing a body reloads; adding or removing one is a restart.</p><p>Materials supplied through <code>define</code> are kept as they are: those came from memory rather than a root, and nothing on disk has an opinion about them.</p><h4 id="returns-4" tabindex="-1">Returns <a class="header-anchor" href="#returns-4" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>boolean</code></td><td>Whether the dispatch was rebuilt.</td></tr><tr><td><code>string</code></td><td>Why not, and nil whenever the first answer is true.</td></tr></tbody></table><p><a id="tecs.gfx.materials.reset"></a></p><h3 id="tecs-gfx-materials-reset" tabindex="-1">tecs.gfx.materials.reset <a class="header-anchor" href="#tecs-gfx-materials-reset" aria-label="Permalink to &quot;tecs.gfx.materials.reset&quot;">​</a></h3><pre><code>function <a href="#tecs.gfx.materials.reset">tecs.gfx.materials.reset</a>()
</code></pre><p>Forgets everything read, so a spec can start from the files again.</p>`,156)])])}const g=e(n,[["render",r]]);export{m as __pageData,g as default};
