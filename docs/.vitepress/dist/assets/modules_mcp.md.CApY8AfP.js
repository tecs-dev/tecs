import{_ as t,o as s,c as a,ag as i}from"./chunks/framework.BPmc_f4o.js";const k=JSON.parse('{"title":"tecs.mcp","description":"The debug server: JSON-RPC over HTTP answered inside the frame, the tools every build exposes, the sandbox and the transport","frontmatter":{"description":"The debug server: JSON-RPC over HTTP answered inside the frame, the tools every build exposes, the sandbox and the transport","outline":"deep"},"headers":[],"relativePath":"modules/mcp.md","filePath":"modules/mcp.md"}'),n={name:"modules/mcp.md"};function o(r,e,d,h,l,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /modules/mcp.md for this page in Markdown format</div><h1 id="tecs-mcp" tabindex="-1">tecs.mcp <a class="header-anchor" href="#tecs-mcp" aria-label="Permalink to &quot;tecs.mcp&quot;">​</a></h1><p><code>tecs.mcp</code> is the debug server. It speaks JSON-RPC over HTTP POST, and everything an agent can do is a tool registered on it: a name, a JSON Schema and a function. The tool list is generated from the same table that dispatches, so there is no second list to keep in step.</p><p>Requests are answered inside <code>update</code>, on the game&#39;s own thread, before the world is stepped. A tool therefore reads a world that is not mid-update and writes one that has not yet advanced, which is why none of them lock and none of them queue anything. The cost is that a tool must not block: it runs during a frame someone is watching.</p><p>The server also outlives the game. When a system raises, <a href="/modules/application.html"><code>Application</code></a> records the traceback here and keeps polling, so an agent that was debugging up to the moment it broke gets the reason rather than a refused connection.</p><h2 id="starting-a-server" tabindex="-1">Starting a server <a class="header-anchor" href="#starting-a-server" aria-label="Permalink to &quot;Starting a server&quot;">​</a></h2><p>Under an application, set <code>Application.Config.mcpPort</code>. Omitting it means no server, since a game should not open a socket nobody asked for. The application binds the renderer and the world to the tools, requires the two tool modules so their registrations run, and calls <code>poll</code> once per frame before the crash guard.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">application</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">create</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">({</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    mcpPort </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">7100</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    plugin </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= registerEverything,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">})</span></span></code></pre></div><h3 id="listen" tabindex="-1">listen <a class="header-anchor" href="#listen" aria-label="Permalink to &quot;listen&quot;">​</a></h3><p>Starts the server on <code>port</code>.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">listen</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(port?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span></span></code></pre></div><p><strong>Parameters:</strong></p><ul><li><code>port</code>: the TCP port. Defaults to <code>7100</code>.</li></ul><p><strong>Returns:</strong> a <code>Server</code>, whose <code>port</code> field is the port it bound. Binding raises rather than retrying on another port: a debugger that silently moved would be worse than one that did not start, since whatever connects to it has only the port it was told.</p><h3 id="server-poll" tabindex="-1">Server:poll <a class="header-anchor" href="#server-poll" aria-label="Permalink to &quot;Server:poll&quot;">​</a></h3><p>Answers at most one request. Call once per frame.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">poll</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span></code></pre></div><p><strong>Returns:</strong> whether a request was dispatched. Accepting and reading never wait, so a frame with nobody connected costs a non-blocking accept.</p><p>A request that is not a <code>POST</code> is answered <code>405 Method Not Allowed</code>. A dispatch that raises is logged and answered <code>500</code> with a JSON-RPC internal error, so a broken request cannot take the frame with it.</p><h3 id="server-destroy" tabindex="-1">Server:destroy <a class="header-anchor" href="#server-destroy" aria-label="Permalink to &quot;Server:destroy&quot;">​</a></h3><p>Releases the server and any connection. Safe to call more than once.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">destroy</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><h2 id="the-protocol" tabindex="-1">The protocol <a class="header-anchor" href="#the-protocol" aria-label="Permalink to &quot;The protocol&quot;">​</a></h2><p>Three methods are answered.</p><table tabindex="0"><thead><tr><th>Method</th><th>Result</th></tr></thead><tbody><tr><td><code>initialize</code></td><td><code>protocolVersion</code> <code>2025-06-18</code>, a <code>tools</code> capability, and <code>serverInfo</code> naming this build</td></tr><tr><td><code>tools/list</code></td><td>every registered tool, in registration order, with its schema and annotations</td></tr><tr><td><code>tools/call</code></td><td>runs the named tool with <code>params.arguments</code> and returns its result</td></tr></tbody></table><p>A tool&#39;s result comes back as <code>structuredContent</code>, the table the handler returned, alongside a <code>content</code> array carrying the same table encoded as text. A tool that raises is reported as a result with <code>isError</code> set and the message as its text content, not as a JSON-RPC error: the agent is told, the frame carries on.</p><p>Each tool is listed with three annotations. <code>readOnlyHint</code> and <code>destructiveHint</code> are MCP&#39;s own; <code>whenCrashedHint</code> is this server&#39;s, so an agent can tell which tools still answer after a crash without calling them.</p><p>JSON-RPC errors use the specification&#39;s codes: <code>-32700</code> for a body that is not JSON, <code>-32600</code> for a request with no method, <code>-32601</code> for an unknown method or an unknown tool name, and <code>-32603</code> for a dispatch that raised.</p><h3 id="dispatch" tabindex="-1">dispatch <a class="header-anchor" href="#dispatch" aria-label="Permalink to &quot;dispatch&quot;">​</a></h3><p>Runs one JSON-RPC request and returns the response text.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">dispatch</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(text: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span></code></pre></div><p>Exposed so the protocol can be exercised without a socket, which is most of what there is to get wrong here.</p><p><strong>Example:</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#702C00;--shiki-dark:#C0CAF5;">curl</span><span style="--shiki-light:#023B95;--shiki-dark:#E0AF68;"> -s</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;"> localhost:7100</span><span style="--shiki-light:#023B95;--shiki-dark:#E0AF68;"> -d</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;"> &#39;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">{&quot;jsonrpc&quot;:&quot;2.0&quot;,&quot;id&quot;:1,&quot;method&quot;:&quot;tools/list&quot;}</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&#39;</span></span></code></pre></div><h3 id="after-a-crash" tabindex="-1">After a crash <a class="header-anchor" href="#after-a-crash" aria-label="Permalink to &quot;After a crash&quot;">​</a></h3><p>While a crash is recorded, every tool that did not declare <code>whenCrashed</code> answers with <code>isError</code>, <code>crashed</code> and the traceback instead of running. A crashed world cannot answer anything about itself, and a tool that read it would return nonsense rather than an error.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">setCrashed</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(traceback: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">crashed</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span></code></pre></div><p><code>crashed</code> returns <code>nil</code> while the game is healthy. The tools that survive a crash are <code>ping</code>, <code>context</code>, <code>get_logs</code> and <code>send_event</code>: reading the log and being able to send a quit are exactly what a stopped process still needs.</p><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;Tools&quot;">​</a></h2><p>The tools below are the ones true of any build. They are registered when their module is required, which the application does for you: <code>tecs.mcp.tools</code> carries the process-facing ones and <code>tecs.mcp.world</code> carries the eight that read and write the world.</p><p>The world tools name a game&#39;s own components on the same terms as the engine&#39;s. They resolve a name through the ECS&#39;s own component registry, so declaring a component is what makes it nameable here and there is no second registration step to find.</p><h3 id="ping" tabindex="-1">ping <a class="header-anchor" href="#ping" aria-label="Permalink to &quot;ping&quot;">​</a></h3><p>Confirms the game is running and answering. Reports a crash rather than refusing to answer after one.</p><p>Read-only, answers after a crash. No arguments.</p><p><strong>Returns:</strong> <code>ok</code>, <code>crashed</code> (the traceback or null) and <code>ticks</code>.</p><h3 id="context" tabindex="-1">context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;context&quot;">​</a></h3><p>What this build is, what it can do, and what the world it is running holds.</p><p>Read-only, answers after a crash. No arguments.</p><p><strong>Returns:</strong> <code>world</code> (entity, archetype, component, declared-component and system counts, or null before a world is bound), the target and architecture, whether JIT and dynamic libraries are available, whether shaders are compiled at runtime or packaged, the shader formats, the core count, the asset and writable roots, the log file path, and every registered logger name.</p><p>Both component counts are reported because they answer different questions: <code>components</code> counts what an archetype in this world carries, while <code>declaredComponents</code> counts the process-wide registry, and the difference is what an agent looking for a component it cannot find needs to see.</p><h3 id="get-logs" tabindex="-1">get_logs <a class="header-anchor" href="#get-logs" aria-label="Permalink to &quot;get_logs&quot;">​</a></h3><p>Reads the log file from a byte offset. Poll with the returned offset to follow it.</p><p>Read-only, answers after a crash.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>after</code></td><td><code>integer</code></td><td>Byte offset to read from. Omit to start at 0.</td></tr><tr><td><code>contains</code></td><td><code>string</code></td><td>Only return lines containing this text.</td></tr></tbody></table><p><strong>Returns:</strong> <code>path</code>, <code>offset</code> and <code>lines</code>. A read covers at most 64 KB and stops on a line boundary, so a cursor never lands mid-record. An <code>after</code> past the end of the file restarts at 0, which is what a truncated file looks like from here. The path is returned so an agent sharing a filesystem with the game can read it directly instead of pulling it through the protocol. See <a href="/modules/log.html#the-log-file"><code>log</code></a> for the record format.</p><h3 id="screenshot" tabindex="-1">screenshot <a class="header-anchor" href="#screenshot" aria-label="Permalink to &quot;screenshot&quot;">​</a></h3><p>Captures the last rendered frame to a PNG and returns its path.</p><p>Read-only.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>string</code></td><td>File name under the writable root. Defaults to <code>screenshot.png</code>.</td></tr></tbody></table><p><strong>Returns:</strong> <code>path</code>, <code>width</code> and <code>height</code>. A file rather than inline base64, because an agent that shares a filesystem with the game reads it far more cheaply and it persists for a human to look at afterwards.</p><h3 id="sample-pixels" tabindex="-1">sample_pixels <a class="header-anchor" href="#sample-pixels" aria-label="Permalink to &quot;sample_pixels&quot;">​</a></h3><p>Reads the colour of specific pixels from the last frame.</p><p>Read-only.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>points</code></td><td><code>array</code></td><td>Points to sample, as <code>{x, y}</code> pairs. Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>width</code>, <code>height</code> and <code>pixels</code>, one entry per point carrying <code>x</code>, <code>y</code> and either <code>r</code>, <code>g</code>, <code>b</code>, <code>a</code> or <code>outside</code> for a point off the frame. A screenshot answers what the frame looks like and needs eyes; this answers whether a pixel is the colour it should be, which is the question an agent can check.</p><h3 id="send-event" tabindex="-1">send_event <a class="header-anchor" href="#send-event" aria-label="Permalink to &quot;send_event&quot;">​</a></h3><p>Pushes an event into the input stream, as if it had come from the platform.</p><p>Destructive, answers after a crash. Calling it with no <code>kind</code> returns the list of kinds instead of pushing anything, and an unknown kind is refused rather than pushed and then ignored. The full event model is in <a href="/modules/events.html"><code>events</code></a>.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>kind</code></td><td><code>string</code></td><td>Event kind. Call with no kind to list them.</td></tr><tr><td><code>scancode</code></td><td><code>integer</code></td><td>For <code>keyDown</code>, <code>keyUp</code>.</td></tr><tr><td><code>button</code></td><td><code>integer</code></td><td>For <code>mouseDown</code>, <code>mouseUp</code>.</td></tr><tr><td><code>x</code>, <code>y</code></td><td><code>number</code></td><td>Position.</td></tr><tr><td><code>clicks</code></td><td><code>integer</code></td><td>Clicks in the run, for <code>mouseDown</code> and <code>mouseUp</code>. One when omitted.</td></tr><tr><td><code>wheelX</code>, <code>wheelY</code></td><td><code>number</code></td><td>For <code>mouseWheel</code>.</td></tr><tr><td><code>wheelTicksX</code>, <code>wheelTicksY</code></td><td><code>integer</code></td><td>Whole notches.</td></tr><tr><td><code>flipped</code></td><td><code>boolean</code></td><td>Send the wheel event as a platform with natural scrolling does, which arrives negated.</td></tr><tr><td><code>penState</code></td><td><code>integer</code></td><td><code>SDL_PEN_INPUT_*</code> mask, for the pen kinds.</td></tr><tr><td><code>repeated</code></td><td><code>boolean</code></td><td>For <code>keyDown</code>.</td></tr><tr><td><code>which</code></td><td><code>integer</code></td><td>Device, window or display the event is about.</td></tr><tr><td><code>data1</code>, <code>data2</code></td><td><code>integer</code></td><td>Payloads, for the window and display kinds.</td></tr><tr><td><code>scale</code></td><td><code>number</code></td><td>Zoom factor, for the pinch kinds.</td></tr><tr><td><code>recording</code></td><td><code>boolean</code></td><td>Whether an audio device records.</td></tr></tbody></table><p><strong>Returns:</strong> <code>pushed</code>, the kind that was pushed, or <code>kinds</code> when called with none.</p><h3 id="audio" tabindex="-1">audio <a class="header-anchor" href="#audio" aria-label="Permalink to &quot;audio&quot;">​</a></h3><p>Sound output: the clips loaded, the voices sounding, and the gains, mutes and pauses over them.</p><p>Read-only. No arguments. Requires an <a href="/modules/audio.html"><code>Audio</code></a> installed on the bound world.</p><p><strong>Returns:</strong> <code>available</code>, <code>masterGain</code>, <code>muted</code>, <code>sounding</code>, <code>maxVoices</code>, <code>loading</code>, the <code>decoders</code> this build linked, and four lists: <code>clips</code>, <code>groups</code>, <code>keys</code> and <code>voices</code>. Sound is the one subsystem with nothing to look at, so what an agent can check is what the layer was asked for: which clips loaded, which voices are sounding and on what, and which of the gains, mutes and pauses over them explains a silence.</p><h3 id="reload-shaders" tabindex="-1">reload_shaders <a class="header-anchor" href="#reload-shaders" aria-label="Permalink to &quot;reload_shaders&quot;">​</a></h3><p>Re-reads shaders and materials from their files and rebuilds the pipelines. Refuses where a reload would change what already-drawn entities mean.</p><p>Destructive. No arguments.</p><p><strong>Returns:</strong> <code>materials</code>, the material names after the reload. It refuses on a build that links no shader compiler, since such a build has no way to change what it draws with while it is running, and on a process with no pipeline rebuild registered, since the sources would be re-read and nothing would draw differently.</p><h3 id="reload-image" tabindex="-1">reload_image <a class="header-anchor" href="#reload-image" aria-label="Permalink to &quot;reload_image&quot;">​</a></h3><p>Re-reads an image file and uploads it over the one already registered under that path. Refuses a file whose size changed.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td><code>string</code></td><td>The path the image was registered under. Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>path</code>, <code>layer</code>, <code>width</code> and <code>height</code>. An image keeps its layer and its rect, so every sprite naming it goes on naming the right texels and the next frame draws the new pixels with nothing in the world touched. A file that changed size cannot do that, so it is refused rather than half applied.</p><h3 id="reload-sound" tabindex="-1">reload_sound <a class="header-anchor" href="#reload-sound" aria-label="Permalink to &quot;reload_sound&quot;">​</a></h3><p>Re-reads a sound file over the clip already loaded from that path. A streamed clip holds nothing, so it reports that rather than replacing anything.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td><code>string</code></td><td>The path the clip was loaded from. Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>path</code>, <code>resident</code> and <code>replaced</code>. A clip is its path, so an edited file comes back under the index every <code>Sound</code> row already carries.</p><h3 id="reload-font" tabindex="-1">reload_font <a class="header-anchor" href="#reload-font" aria-label="Permalink to &quot;reload_font&quot;">​</a></h3><p>Re-reads a font&#39;s metrics over the font already loaded from that path, and lays out every text drawing it again. Refuses metrics that name an atlas of a different size.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td><code>string</code></td><td>The path the font&#39;s metrics were loaded from. Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>path</code>, <code>font</code> and <code>glyphs</code>. This is the one reload with something in the world to put right afterwards: a glyph is an instance laid out once from the metrics and left alone until its text changes, so re-reading the metrics has to lay those glyphs out again.</p><h3 id="watch" tabindex="-1">watch <a class="header-anchor" href="#watch" aria-label="Permalink to &quot;watch&quot;">​</a></h3><p>Reports, starts, stops or steps the content file watcher, which reloads shaders, images, sounds and fonts when their files change.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>boolean</code></td><td>Starts or stops the watcher. Omit to leave it as it is.</td></tr><tr><td><code>poll</code></td><td><code>boolean</code></td><td>Looks at every watched file now, without waiting for the interval.</td></tr></tbody></table><p><strong>Returns:</strong> <code>watching</code>, the number of <code>files</code> and their <code>paths</code>, the <code>unsettled</code> ones, the <code>kinds</code> registered, <code>dispatched</code> and how many reloads <code>fired</code> on this call. It refuses on a build that links no shader compiler, which is a release and has no business polling the filesystem. Polling from here as well as from the loop is what lets an agent that has just written a file wait for the effect rather than for the interval. See <a href="/modules/filesystem/watch.html"><code>watch</code></a>.</p><p><code>unsettled</code> names the files that have changed and have not been dispatched yet. A file has to report the same size and modification time on consecutive polls first, because an editor saving commonly truncates and rewrites, and handing a half-written file to a reloader is how a watcher takes a process down. So a <code>fired</code> of zero with the path still in <code>unsettled</code> means the write is still landing: poll again rather than conclude nothing happened.</p><h3 id="components-info" tabindex="-1">components_info <a class="header-anchor" href="#components-info" aria-label="Permalink to &quot;components_info&quot;">​</a></h3><p>Lists the components these tools can name, with the fields each carries and whether the bound world carries any.</p><p>Read-only. No arguments.</p><p><strong>Returns:</strong> <code>components</code>, one entry per registered component with its <code>name</code>, <code>id</code>, whether it is a <code>tag</code>, its <code>fields</code>, and whether the bound world has any entity carrying it; plus <code>bound</code>. The registry is process-wide and a world is not, so a component declared here and carried by nothing is an ordinary state rather than a name that does not exist. Fields are read from a default instance rather than declared again, so this cannot describe a component incorrectly.</p><h3 id="query" tabindex="-1">query <a class="header-anchor" href="#query" aria-label="Permalink to &quot;query&quot;">​</a></h3><p>Finds entities carrying every named component. Returns their ids and, unless <code>values</code> is false, their component values.</p><p>Read-only.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>include</code></td><td><code>array</code></td><td>Component names an entity must have. Required.</td></tr><tr><td><code>limit</code></td><td><code>integer</code></td><td>Default 50.</td></tr><tr><td><code>values</code></td><td><code>boolean</code></td><td>Include component values. Default true.</td></tr></tbody></table><p><strong>Returns:</strong> <code>matched</code>, the total number of entities the query covered, <code>returned</code>, and <code>entities</code>, capped at <code>limit</code>. Each row carries the entity id, its <code>Name</code> where it has one, and its component values.</p><h3 id="info" tabindex="-1">info <a class="header-anchor" href="#info" aria-label="Permalink to &quot;info&quot;">​</a></h3><p>Everything known about one entity.</p><p>Read-only.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>entity</code></td><td><code>integer</code></td><td>Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>entity</code>, <code>alive</code>, <code>name</code>, <code>components</code> and <code>count</code>. An entity that is not alive answers with <code>alive</code> false and nothing else.</p><p>Component values cross as plain tables through each component&#39;s own <code>serialize</code>, so an FFI component arrives as numbers rather than as cdata JSON cannot carry. A tag has no data, so it reports as <code>true</code> rather than as an empty object a client would have to special-case.</p><h3 id="spawn" tabindex="-1">spawn <a class="header-anchor" href="#spawn" aria-label="Permalink to &quot;spawn&quot;">​</a></h3><p>Creates an entity carrying the given components.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>components</code></td><td><code>object</code></td><td>Component name to its field values. Required.</td></tr></tbody></table><p><strong>Returns:</strong> the new entity, described as <a href="/modules/mcp.html#info"><code>info</code></a> describes one.</p><h3 id="set" tabindex="-1">set <a class="header-anchor" href="#set" aria-label="Permalink to &quot;set&quot;">​</a></h3><p>Replaces a component in full, adding it if absent.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>entity</code></td><td><code>integer</code></td><td>Required.</td></tr><tr><td><code>component</code></td><td><code>string</code></td><td>Required.</td></tr><tr><td><code>values</code></td><td><code>object</code></td><td>Field values.</td></tr></tbody></table><p><strong>Returns:</strong> the entity as <code>info</code> describes it, plus <code>added</code>. Instances are built through the component&#39;s own constructor, so a field the payload omits takes the component&#39;s default. That is what makes this a replacement rather than a merge; prefer <code>modify</code> for changing one value without disturbing the rest.</p><h3 id="modify" tabindex="-1">modify <a class="header-anchor" href="#modify" aria-label="Permalink to &quot;modify&quot;">​</a></h3><p>Changes only the named fields, leaving the rest alone.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>entity</code></td><td><code>integer</code></td><td>Required.</td></tr><tr><td><code>component</code></td><td><code>string</code></td><td>Required.</td></tr><tr><td><code>values</code></td><td><code>object</code></td><td>Fields to change. Required.</td></tr></tbody></table><p><strong>Returns:</strong> the entity as <code>info</code> describes it, plus <code>skipped</code>. An entity that is not alive or does not carry the component is skipped rather than gaining one, so a typo cannot silently add a component. The write goes through <code>getMut</code>, so the column is marked dirty and whatever consumes it re-uploads.</p><h3 id="remove" tabindex="-1">remove <a class="header-anchor" href="#remove" aria-label="Permalink to &quot;remove&quot;">​</a></h3><p>Removes a component from an entity.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>entity</code></td><td><code>integer</code></td><td>Required.</td></tr><tr><td><code>component</code></td><td><code>string</code></td><td>Required.</td></tr></tbody></table><p><strong>Returns:</strong> the entity as <code>info</code> describes it, plus <code>skipped</code>.</p><h3 id="despawn" tabindex="-1">despawn <a class="header-anchor" href="#despawn" aria-label="Permalink to &quot;despawn&quot;">​</a></h3><p>Destroys an entity.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>entity</code></td><td><code>integer</code></td><td>Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>entity</code> and <code>skipped</code>.</p><h3 id="run-lua" tabindex="-1">run_lua <a class="header-anchor" href="#run-lua" aria-label="Permalink to &quot;run_lua&quot;">​</a></h3><p>Runs Lua with <code>world</code> in scope. Returns an envelope <code>{returned, values}</code>. State persists between calls, so a handle stashed in a global is there next time. Prefer a structured tool where one covers the operation.</p><p>Destructive.</p><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>code</code></td><td><code>string</code></td><td>Lua to run. A bare expression is returned. Required.</td></tr></tbody></table><p><strong>Returns:</strong> <code>returned</code>, the number of values the chunk returned, and <code>values</code>, each converted into something JSON can carry. The count is reported separately because returning <code>nil</code> has to be distinguishable from returning nothing. Execution is synchronous, inside the frame, so there is no deferral and no second round trip.</p><h2 id="registering-your-own-tools" tabindex="-1">Registering your own tools <a class="header-anchor" href="#registering-your-own-tools" aria-label="Permalink to &quot;Registering your own tools&quot;">​</a></h2><p>A game registers what its own world needs. The built-in set is deliberately small: what is true of any build, and what an agent reaches for before it knows anything else.</p><h3 id="tool" tabindex="-1">Tool <a class="header-anchor" href="#tool" aria-label="Permalink to &quot;Tool&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">record</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Tool</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    name</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    description</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    inputSchema</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    handler</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Handler</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    readOnly</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    destructive</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    whenCrashed</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span></code></pre></div><p><strong>Fields:</strong></p><ul><li><code>name</code>: the tool name, which is what <code>tools/call</code> dispatches on.</li><li><code>description</code>: sent in the tool list.</li><li><code>inputSchema</code>: JSON Schema for the arguments, sent verbatim. A tool with no arguments uses <code>{ type = &quot;object&quot;, properties = tecs.data.empty_array }</code>, because an empty Lua table encodes as an object and a client parsing the list strictly would reject it.</li><li><code>handler</code>: the function. Arguments arrive decoded; the table it returns is encoded as the structured content.</li><li><code>readOnly</code>: declared so an agent can tell what a call will do before making it. False by default.</li><li><code>destructive</code>: the same, for a call that changes something. False by default.</li><li><code>whenCrashed</code>: still callable after the game has crashed. False by default, because a crashed world cannot answer anything about itself.</li></ul><h3 id="handler" tabindex="-1">Handler <a class="header-anchor" href="#handler" aria-label="Permalink to &quot;Handler&quot;">​</a></h3><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">type</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Handler</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> = </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(arguments: </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span></code></pre></div><p>Raising from a handler is how a tool reports a refusal. Use <code>error(message, 0)</code> so the message reaches the agent without a file and line in front of it.</p><h3 id="register" tabindex="-1">register <a class="header-anchor" href="#register" aria-label="Permalink to &quot;register&quot;">​</a></h3><p>Registers a tool. Re-registering a name replaces it.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">register</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(tool: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Tool</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p>Raises when the tool has no <code>name</code> or no <code>handler</code>.</p><p><strong>Example:</strong></p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">tecs.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">register</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">({</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    name </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">spawn_wave</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    description </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">Spawns a wave of enemies and returns how many were created.</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    destructive </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">true</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    inputSchema </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= {</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">        type </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">object</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">        properties </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= { </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">count </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= { </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">type </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">integer</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> } },</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">        required </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= { </span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">count</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> },</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">    },</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    handler </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(args: </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">        local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> count </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> math.floor</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">tonumber</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(args.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">count</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">) </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">or</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;"> 0</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">) </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">as</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> integer</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">        if</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> count </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">&lt;</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;"> 1</span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;"> then</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> error</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#032563;--shiki-dark:#9ECE6A;">count must be at least one</span><span style="--shiki-light:#032563;--shiki-dark:#89DDFF;">&quot;</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;">0</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">) </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">        for</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> _ </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;"> 1</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, count </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">do</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> spawnEnemy</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(world) </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">        return</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> { </span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">spawned </span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">= count }</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    end</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">,</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">})</span></span></code></pre></div><h3 id="tools-1" tabindex="-1">tools <a class="header-anchor" href="#tools-1" aria-label="Permalink to &quot;tools&quot;">​</a></h3><p>Every registered tool, in registration order.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> mcp</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">mcp</span><span style="--shiki-light:#023B95;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Tool</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span></code></pre></div><h2 id="binding" tabindex="-1">Binding <a class="header-anchor" href="#binding" aria-label="Permalink to &quot;Binding&quot;">​</a></h2><p>The tools are bound to a renderer and a world rather than to an application, so a tool reads the same world whatever a game registered. <code>Application</code> calls all three of these; a test or a tool assembling the pieces by hand calls them itself.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">bind</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(renderer: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Renderer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, world: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">ecs</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">World</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">bindReload</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(rebuild: </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">())</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> world</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">bind</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(w: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">ecs</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">World</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span></code></pre></div><p><code>tools.bind</code> supplies what the frame-facing tools look at. <code>tools.bindReload</code> supplies the device&#39;s half of a shader reload: swapping a pipeline means destroying live GPU handles and creating new ones against the formats the device claimed, which is knowledge the <a href="/modules/gfx/"><code>Renderer</code></a> has and nothing in the tools does. <code>world.bind</code> is what the eight world tools act on. A tool called before its binding exists reports why rather than failing obscurely.</p><h3 id="the-reloads" tabindex="-1">The reloads <a class="header-anchor" href="#the-reloads" aria-label="Permalink to &quot;The reloads&quot;">​</a></h3><p>The four reloads are functions first and tools second, because a saved file arriving at the file watcher and an agent asking have to be refused on identical terms or the two disagree about what the process is drawing. Each raises with the reason when it refuses.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reloadShaders</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(): </span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">{</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#023B95;--shiki-dark:#A9B1D6;">}</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reloadImage</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reloadSound</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> tools</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reloadFont</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(path: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span></span></code></pre></div><p><code>reloadShaders</code> returns the material names. <code>reloadImage</code> returns the layer the image occupies and its width and height. <code>reloadSound</code> returns whether the clip is held in memory, which is what was replaced. <code>reloadFont</code> returns the name the font is loaded under and how many glyphs it carries.</p><h2 id="the-sandbox" tabindex="-1">The sandbox <a class="header-anchor" href="#the-sandbox" aria-label="Permalink to &quot;The sandbox&quot;">​</a></h2><p><code>tecs.mcp.sandbox</code> is the environment <code>run_lua</code> compiles into. It is kept out of the tool so protocol dispatch carries no policy, and it holds the environment across calls, so an agent can stash a handle in one call and read it in the next. That is what makes an exploratory session possible at all.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> sandbox</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">compile</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(code: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">...</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> sandbox</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">reset</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> sandbox</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">describe</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(value: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, depth?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">any</span></span></code></pre></div><p><code>compile</code> wraps the code so <code>world</code> is a parameter and both a statement block and a bare expression work: a chunk that does not compile is retried with <code>return</code> in front of it before a syntax error is reported. It returns <code>nil</code> and a diagnostic when neither form compiles. <code>reset</code> forgets everything a previous call stashed. <code>describe</code> converts a value into something JSON can carry, because encoding raises on a function, a coroutine or cdata, and an agent that returned one by accident should get a description of it rather than a failed call; it walks 6 levels deep and 256 entries per table before summarising.</p><p>The environment carries <code>assert</code>, <code>error</code>, <code>ipairs</code>, <code>next</code>, <code>pairs</code>, <code>pcall</code>, <code>print</code>, <code>select</code>, <code>setmetatable</code>, <code>getmetatable</code>, <code>rawget</code>, <code>rawset</code>, <code>rawequal</code>, <code>rawlen</code>, <code>tonumber</code>, <code>tostring</code>, <code>type</code>, <code>unpack</code>, <code>xpcall</code>, <code>require</code>, <code>string</code>, <code>table</code>, <code>math</code>, <code>coroutine</code>, <code>bit</code>, <code>jit</code> and <code>ffi</code>, plus an <code>os</code> holding only <code>time</code>, <code>clock</code>, <code>date</code>, <code>difftime</code> and <code>getenv</code>. Absent on purpose: <code>io</code>, <code>dofile</code>, <code>loadfile</code>, <code>load</code>, and <code>os.execute</code>, <code>remove</code>, <code>rename</code> and <code>exit</code>. Anything not on the list is absent, so a new global in the host does not silently become reachable.</p><div class="warning custom-block"><p class="custom-block-title">A guard rail, not a security boundary</p><p><code>run_lua</code> exists to do what no structured tool covers, so anything reachable from the engine is reachable from it. What is removed are the operations that damage the machine rather than the game.</p></div><h2 id="the-transport" tabindex="-1">The transport <a class="header-anchor" href="#the-transport" aria-label="Permalink to &quot;The transport&quot;">​</a></h2><p><code>tecs.mcp.transport</code> is HTTP framing over SDL3_net, polled rather than blocking, because the thing being debugged is a game loop and a transport that blocked on accept would stall the frame it exists to observe.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">record</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> Request</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    method</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    path</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">    body</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">listen</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(port: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">poll</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(now: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">number</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, handler: </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#89DDFF;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Request</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)): </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">boolean</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">respond</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(status: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">integer</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, reason: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, body: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, contentType?: </span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">string</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">close</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">function</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;"> transport</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">.</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">Server</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">destroy</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">()</span></span></code></pre></div><p><code>poll</code> accepts and reads without waiting and calls <code>handler</code> only with a whole request; <code>now</code> is the engine clock and is used only to time a connection out. <code>respond</code> writes the status line, a <code>Content-Type</code> that defaults to <code>application/json</code>, a <code>Content-Length</code> and the body, drains the socket and closes, since each request gets its own connection.</p><p>Only <code>Content-Length</code> framing is handled, because MCP posts a JSON body and always declares its length. One connection is served at a time: a debugger is one client, and a queue of pending sockets would be state to reason about for no benefit. A request arrives over as many reads as it takes, 16 KB at a time, and a connection that stops mid-request is dropped after 10 seconds so a half-open peer cannot hold the single client slot forever.</p><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to &quot;Reference&quot;">​</a></h2><p>Every function and type this module carries, rendered from <code>src/tecs/mcp/init.tl</code>.</p><p><a id="tecs.mcp.Handler"></a></p><h3 id="tecs-mcp-handler" tabindex="-1">tecs.mcp.Handler <a class="header-anchor" href="#tecs-mcp-handler" aria-label="Permalink to &quot;tecs.mcp.Handler&quot;">​</a></h3><pre><code>type <a href="#tecs.mcp.Handler">tecs.mcp.Handler</a> = function({string : &lt;any type&gt;}): {string : &lt;any type&gt;}
</code></pre><p>What a tool receives and returns. Arguments arrive decoded; the result is encoded as the tool&#39;s structured content. <a id="tecs.mcp.Server"></a></p><h3 id="tecs-mcp-server" tabindex="-1">tecs.mcp.Server <a class="header-anchor" href="#tecs-mcp-server" aria-label="Permalink to &quot;tecs.mcp.Server&quot;">​</a></h3><pre><code>record <a href="#tecs.mcp.Server">tecs.mcp.Server</a>
</code></pre><p>A listening MCP endpoint. Answers only while something calls <code>poll</code>. <a id="tecs.mcp.Server.port"></a></p><h3 id="tecs-mcp-server-port" tabindex="-1">tecs.mcp.Server.port <a class="header-anchor" href="#tecs-mcp-server-port" aria-label="Permalink to &quot;tecs.mcp.Server.port&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Server.port">tecs.mcp.Server.port</a>: integer
</code></pre><p>The TCP port bound, which is 7100 when <code>listen</code> was given none. <a id="tecs.mcp.Server.poll"></a></p><h3 id="tecs-mcp-server-poll" tabindex="-1">tecs.mcp.Server.poll <a class="header-anchor" href="#tecs-mcp-server-poll" aria-label="Permalink to &quot;tecs.mcp.Server.poll&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.Server.poll">tecs.mcp.Server.poll</a>(self: <a href="#tecs.mcp.Server">Server</a>): boolean
</code></pre><p>Answers at most one request. Call once per frame.</p><p>Reads the clock itself, so no time is passed in. Only POST is accepted; anything else is answered 405 without reaching <code>dispatch</code>.</p><h4 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.mcp.Server">Server</a></code></td><td><code>self</code></td><td></td></tr></tbody></table><h4 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>boolean</code></td><td>True when a request was answered, which includes one answered with a 405 or a JSON-RPC error. False when nothing was waiting, when a request is still arriving, or when the server has been destroyed.</td></tr></tbody></table><p><a id="tecs.mcp.Server.destroy"></a></p><h3 id="tecs-mcp-server-destroy" tabindex="-1">tecs.mcp.Server.destroy <a class="header-anchor" href="#tecs-mcp-server-destroy" aria-label="Permalink to &quot;tecs.mcp.Server.destroy&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.Server.destroy">tecs.mcp.Server.destroy</a>(self: <a href="#tecs.mcp.Server">Server</a>)
</code></pre><p>Releases the server. Safe to call more than once.</p><p>One way: the port is given up and the server cannot be made to listen again. The tool registry is module-wide and is left untouched, so a later <code>listen</code> answers the same tools.</p><h4 id="parameters-1" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-1" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code><a href="#tecs.mcp.Server">Server</a></code></td><td><code>self</code></td><td></td></tr></tbody></table><p><a id="tecs.mcp.Tool"></a></p><h3 id="tecs-mcp-tool" tabindex="-1">tecs.mcp.Tool <a class="header-anchor" href="#tecs-mcp-tool" aria-label="Permalink to &quot;tecs.mcp.Tool&quot;">​</a></h3><pre><code>record <a href="#tecs.mcp.Tool">tecs.mcp.Tool</a>
</code></pre><p>One registered tool: what it is called, how it is described to an agent, and what runs when the agent calls it. <a id="tecs.mcp.Tool.name"></a></p><h3 id="tecs-mcp-tool-name" tabindex="-1">tecs.mcp.Tool.name <a class="header-anchor" href="#tecs-mcp-tool-name" aria-label="Permalink to &quot;tecs.mcp.Tool.name&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.name">tecs.mcp.Tool.name</a>: string
</code></pre><p>The name an agent calls, and the registry key. Established MCP tool names are spelled the way the protocol&#39;s ecosystem spells them, <code>run_lua</code> and <code>send_event</code> among them, so they are snake_case where the rest of this tree is camelCase; that is deliberate and is not to be renamed. Required. <a id="tecs.mcp.Tool.description"></a></p><h3 id="tecs-mcp-tool-description" tabindex="-1">tecs.mcp.Tool.description <a class="header-anchor" href="#tecs-mcp-tool-description" aria-label="Permalink to &quot;tecs.mcp.Tool.description&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.description">tecs.mcp.Tool.description</a>: string
</code></pre><p>One line shown to the agent alongside the name. Optional: nil is sent as an empty string rather than omitted. <a id="tecs.mcp.Tool.inputSchema"></a></p><h3 id="tecs-mcp-tool-inputschema" tabindex="-1">tecs.mcp.Tool.inputSchema <a class="header-anchor" href="#tecs-mcp-tool-inputschema" aria-label="Permalink to &quot;tecs.mcp.Tool.inputSchema&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.inputSchema">tecs.mcp.Tool.inputSchema</a>: {string : &lt;any type&gt;}
</code></pre><p>JSON Schema for the arguments, sent verbatim in the tool list. <a id="tecs.mcp.Tool.handler"></a></p><h3 id="tecs-mcp-tool-handler" tabindex="-1">tecs.mcp.Tool.handler <a class="header-anchor" href="#tecs-mcp-tool-handler" aria-label="Permalink to &quot;tecs.mcp.Tool.handler&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.handler">tecs.mcp.Tool.handler</a>: <a href="#tecs.mcp.Handler">Handler</a>
</code></pre><p>Runs on the game thread when the tool is called. Required, and its absence raises at registration rather than at call time. <a id="tecs.mcp.Tool.readOnly"></a></p><h3 id="tecs-mcp-tool-readonly" tabindex="-1">tecs.mcp.Tool.readOnly <a class="header-anchor" href="#tecs-mcp-tool-readonly" aria-label="Permalink to &quot;tecs.mcp.Tool.readOnly&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.readOnly">tecs.mcp.Tool.readOnly</a>: boolean
</code></pre><p>Declared so an agent can tell what a call will do before making it. <a id="tecs.mcp.Tool.destructive"></a></p><h3 id="tecs-mcp-tool-destructive" tabindex="-1">tecs.mcp.Tool.destructive <a class="header-anchor" href="#tecs-mcp-tool-destructive" aria-label="Permalink to &quot;tecs.mcp.Tool.destructive&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.destructive">tecs.mcp.Tool.destructive</a>: boolean
</code></pre><p>Whether a call changes state that a caller would not want changed without asking. Optional, false when nil. Advisory, like <code>readOnly</code>: both are reported to the agent and neither is checked here, so a tool declared read-only is still free to write. <a id="tecs.mcp.Tool.whenCrashed"></a></p><h3 id="tecs-mcp-tool-whencrashed" tabindex="-1">tecs.mcp.Tool.whenCrashed <a class="header-anchor" href="#tecs-mcp-tool-whencrashed" aria-label="Permalink to &quot;tecs.mcp.Tool.whenCrashed&quot;">​</a></h3><pre><code><a href="#tecs.mcp.Tool.whenCrashed">tecs.mcp.Tool.whenCrashed</a>: boolean
</code></pre><p>Still callable after the game has crashed. False by default, because a crashed world cannot answer anything about itself, and a tool that read it would return nonsense rather than an error. <a id="tecs.mcp.crashed"></a></p><h3 id="tecs-mcp-crashed" tabindex="-1">tecs.mcp.crashed <a class="header-anchor" href="#tecs-mcp-crashed" aria-label="Permalink to &quot;tecs.mcp.crashed&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.crashed">tecs.mcp.crashed</a>(): string
</code></pre><p>The crash traceback, or nil while the game is healthy.</p><h4 id="returns-1" tabindex="-1">Returns <a class="header-anchor" href="#returns-1" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td>nil means no crash has been recorded, which is the only sense in which the game is known to be healthy: nothing here polls for one.</td></tr></tbody></table><p><a id="tecs.mcp.dispatch"></a></p><h3 id="tecs-mcp-dispatch" tabindex="-1">tecs.mcp.dispatch <a class="header-anchor" href="#tecs-mcp-dispatch" aria-label="Permalink to &quot;tecs.mcp.dispatch&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.dispatch">tecs.mcp.dispatch</a>(text: string): string
</code></pre><p>Runs one JSON-RPC request and returns the response text.</p><p>Exposed so the protocol can be tested without a socket, which is most of what there is to get wrong here.</p><p>The three methods answered, <code>initialize</code>, <code>tools/list</code> and <code>tools/call</code>, are spelled as the MCP specification spells them and are not this tree&#39;s to rename. Anything else is a method-not-found error.</p><p>A handler that raises is caught: the agent is told through <code>isError</code> and the server carries on. A handler that returns nil is not an error, and comes back as an empty <code>structuredContent</code>.</p><h4 id="parameters-2" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-2" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>text</code></td><td>One JSON-RPC request object. A batch, that is a JSON array, is not supported; it decodes, has no <code>method</code>, and comes back as an invalid-request error rather than being fanned out.</td></tr></tbody></table><h4 id="returns-2" tabindex="-1">Returns <a class="header-anchor" href="#returns-2" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td>Always a response as JSON text, never nil, and never a raise, since bad input is reported in-band. A request that carried no <code>id</code> is still answered, with the <code>id</code> field absent from the response.</td></tr></tbody></table><p><a id="tecs.mcp.listen"></a></p><h3 id="tecs-mcp-listen" tabindex="-1">tecs.mcp.listen <a class="header-anchor" href="#tecs-mcp-listen" aria-label="Permalink to &quot;tecs.mcp.listen&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.listen">tecs.mcp.listen</a>(port: integer): mcp.Server
</code></pre><p>Starts the server on <code>port</code>.</p><h4 id="parameters-3" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-3" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>integer</code></td><td><code>port</code></td><td>Defaults to 7100. A port already in use raises rather than being worked around, since a debugger that moved could not be found.</td></tr></tbody></table><h4 id="returns-3" tabindex="-1">Returns <a class="header-anchor" href="#returns-3" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>mcp.Server</code></td><td>Listening, but answering nothing until something calls <code>poll</code>.</td></tr></tbody></table><p><a id="tecs.mcp.register"></a></p><h3 id="tecs-mcp-register" tabindex="-1">tecs.mcp.register <a class="header-anchor" href="#tecs-mcp-register" aria-label="Permalink to &quot;tecs.mcp.register&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.register">tecs.mcp.register</a>(tool: mcp.Tool)
</code></pre><p>Registers a tool. Re-registering a name replaces it.</p><p>Replacing keeps the name&#39;s original position in the list, so the order a client sees is first-registration order rather than last.</p><h4 id="parameters-4" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-4" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>mcp.Tool</code></td><td><code>tool</code></td><td>Stored by reference and never copied, so mutating the table afterwards changes what the server dispatches and reports. <code>name</code> and <code>handler</code> are required and their absence raises; the rest may be nil.</td></tr></tbody></table><p><a id="tecs.mcp.setCrashed"></a></p><h3 id="tecs-mcp-setcrashed" tabindex="-1">tecs.mcp.setCrashed <a class="header-anchor" href="#tecs-mcp-setcrashed" aria-label="Permalink to &quot;tecs.mcp.setCrashed&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.setCrashed">tecs.mcp.setCrashed</a>(traceback: string)
</code></pre><p>Records a crash. Every subsequent world-touching call reports it.</p><h4 id="parameters-5" tabindex="-1">Parameters <a class="header-anchor" href="#parameters-5" aria-label="Permalink to &quot;Parameters&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Name</th><th>Description</th></tr></thead><tbody><tr><td><code>string</code></td><td><code>traceback</code></td><td>Stored as given and handed back verbatim by <code>crashed</code>, and sent as the whole body of the error a blocked tool answers with. Passing nil clears the crashed state and lets every tool run again.</td></tr></tbody></table><p><a id="tecs.mcp.tools"></a></p><h3 id="tecs-mcp-tools" tabindex="-1">tecs.mcp.tools <a class="header-anchor" href="#tecs-mcp-tools" aria-label="Permalink to &quot;tecs.mcp.tools&quot;">​</a></h3><pre><code>function <a href="#tecs.mcp.tools">tecs.mcp.tools</a>(): {mcp.Tool}
</code></pre><p>Every registered tool, in registration order.</p><h4 id="returns-4" tabindex="-1">Returns <a class="header-anchor" href="#returns-4" aria-label="Permalink to &quot;Returns&quot;">​</a></h4><table tabindex="0"><thead><tr><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>{mcp.Tool}</code></td><td>A fresh list each call, so the caller may keep it. The <code>Tool</code> values in it are the registered tables themselves rather than copies, so writing to one writes to the registry.</td></tr></tbody></table>`,276)])])}const g=t(n,[["render",o]]);export{k as __pageData,g as default};
