import{_ as t,o as i,c as s,ag as a}from"./chunks/framework.BPmc_f4o.js";const k=JSON.parse('{"title":"Dirty Tracking","description":"Per-archetype per-component dirty bits set by getMut and set, and the extractor that reads them","frontmatter":{"description":"Per-archetype per-component dirty bits set by getMut and set, and the extractor that reads them","outline":"deep"},"headers":[],"relativePath":"ecs/components/dirty-tracking.md","filePath":"ecs/components/dirty-tracking.md"}'),n={name:"ecs/components/dirty-tracking.md"};function o(r,e,h,l,d,c){return i(),s("div",null,[...e[0]||(e[0]=[a(`<div style="display:none;" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /ecs/components/dirty-tracking.md for this page in Markdown format</div><h1 id="dirty-tracking" tabindex="-1">Dirty Tracking <a class="header-anchor" href="#dirty-tracking" aria-label="Permalink to &quot;Dirty Tracking&quot;">​</a></h1><p>Tecs tracks dirty state per archetype, per component. A column whose bytes may have changed since the last frame is <em>dirty</em>, and that bit is the signal an incremental consumer uses to decide what to re-process. The model is deliberately coarse: granularity stops at the archetype plus component level. There is no per-row dirty bit.</p><p>If you need a refresher on how component values are created and replaced, start with <a href="/ecs/components/construction.html">Component Construction</a>. For the component categories this page mentions in passing, see the <a href="/ecs/components/">Components overview</a>. The <a href="/ecs/mutation-model.html">mutation model</a> is the normative specification of which paths mark what; this page is the reader&#39;s side of it.</p><h2 id="who-reads-the-bits" tabindex="-1">Who reads the bits <a class="header-anchor" href="#who-reads-the-bits" aria-label="Permalink to &quot;Who reads the bits&quot;">​</a></h2><p>The extractor, the world-facing half of rendering, is the consumer that makes this model matter. Once per frame it walks the archetypes holding renderable entities and, for each one, asks whether any of <code>Transform</code>, <code>Tint</code>, <code>Sprite</code>, <code>Material</code>, <code>Clip</code> or the sprite sheet&#39;s <code>Pivot</code> is dirty. Only when one of them is (or when the archetype&#39;s rows moved within the instance buffer, or an interpolated entity&#39;s blend factor changed) does it rewrite that archetype&#39;s run of GPU instances.</p><p>That gate is what makes a large world affordable: most frames change very little, and an archetype nothing touched costs a handful of bit tests instead of a rewrite of every row it holds. It also sets the contract in the other direction. A write the framework cannot see is a write the extractor will not notice, and the entity keeps drawing where it used to be.</p><h2 id="what-sets-a-dirty-bit" tabindex="-1">What sets a dirty bit <a class="header-anchor" href="#what-sets-a-dirty-bit" aria-label="Permalink to &quot;What sets a dirty bit&quot;">​</a></h2><p>A component is marked dirty on its archetype when:</p><ul><li><strong><code>archetype:getMut(Foo)</code></strong> is called, inside a query iteration or anywhere else with an archetype handle. This is the primary signal: fetching the column for write declares mutation intent. It is idempotent, so N writes behind one <code>getMut</code> collapse to one mark.</li><li><strong><code>world:getMut(entity, Foo)</code></strong> is the per-entity equivalent. Use it whenever you would otherwise write <code>local t = world:get(...)</code> and then mutate <code>t</code>.</li><li><strong><code>world:set(entity, Foo(...))</code></strong> writes a value, including the three-argument scalar form <code>world:set(entity, Foo, value)</code>.</li><li><strong><code>archetype:set(row, Foo(...))</code></strong> replaces a single row&#39;s value.</li><li><strong><code>world:markComponentDirty(entity, Foo)</code></strong> is called explicitly.</li><li><strong>Spawn, archetype move-in, and swap-pop</strong>: every component on the affected archetype is flagged in one bulk operation, because every column has new bytes for at least one row.</li></ul><p>Bits are cleared at the end of each <code>world:update</code>, after the pipeline has run.</p><div class="warning custom-block"><p class="custom-block-title">A <code>get</code> plus a cdata write is invisible</p><p><code>world:get</code> on an FFI component hands back a cdata reference. Writing through it changes the bytes without any assignment the framework can observe, so nothing is marked and the GPU never re-syncs. Either fetch with <code>world:getMut</code>, or call <code>world:markComponentDirty(id, Component)</code> after the write.</p></div><h2 id="reading-and-writing-columns" tabindex="-1">Reading and writing columns <a class="header-anchor" href="#reading-and-writing-columns" aria-label="Permalink to &quot;Reading and writing columns&quot;">​</a></h2><p>Two access primitives, distinguished by intent:</p><ul><li><strong><code>archetype:get(Foo)</code></strong>: returns the column (or <code>nil</code> if the archetype doesn&#39;t carry <code>Foo</code>). Does not mark dirty.</li><li><strong><code>archetype:getMut(Foo)</code></strong>: same return; also marks <code>Foo</code> dirty on the archetype.</li></ul><p>Use <code>:get</code> at read sites and <code>:getMut</code> at every site you intend to write into the column, whether the write goes through table assignment or an FFI cdata field. The dirty machinery cannot observe the write itself; the <code>:getMut</code> call is the contract that tells consumers &quot;this column may have changed&quot;.</p><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">for</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> archetype, len </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">in</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> query:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">iter</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">() </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">do</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">    local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> transforms </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> archetype:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">getMut</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(Transform)</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">    local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> velocities </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> archetype:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">get</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(Velocity)  </span><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">-- read-only</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    for</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> row </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#023B95;--shiki-dark:#FF9E64;"> 1</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">, len </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">do</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">        local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> t </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> transforms[row]</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-light-font-style:inherit;--shiki-dark:#9D7CD8;--shiki-dark-font-style:italic;">        local</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> v </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">=</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> velocities[row]</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">        t.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">x</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> t.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">x</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> +</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> v.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">vx</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> *</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> dt</span></span>
<span class="line"><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">        t.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">y</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> =</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> t.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">y</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> +</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> v.</span><span style="--shiki-light:#0E1116;--shiki-dark:#C0CAF5;">vy</span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;"> *</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> dt</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    end</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span></code></pre></div><p>Both <code>:get</code> and <code>:getMut</code> return the same underlying column, so mixing them in one loop is safe; only the dirty marks differ.</p><div class="danger custom-block"><p class="custom-block-title">Don&#39;t <code>getMut</code> speculatively</p><p>Never call <code>getMut</code> at the top of a loop that might not write. It marks the whole column dirty on that archetype, which defeats every dirty-gated consumer downstream: the extractor will rewrite every row of that archetype, every frame, for nothing. If the write is conditional, hoist the condition, or bind with <code>:get</code> and switch to <code>:getMut</code> on the branch that writes.</p></div><h2 id="checking-dirty-state" tabindex="-1">Checking dirty state <a class="header-anchor" href="#checking-dirty-state" aria-label="Permalink to &quot;Checking dirty state&quot;">​</a></h2><ul><li><strong><code>archetype:isComponentDirty(Foo)</code></strong>: true when <code>Foo</code> was marked dirty on this archetype since the last frame. False when the archetype doesn&#39;t carry <code>Foo</code> at all.</li><li><strong><code>archetype:anyComponentDirty()</code></strong>: true when any component on this archetype is currently dirty. Useful for bulk re-sync paths that don&#39;t need per-component granularity.</li><li><strong><code>archetype:dirtyComponents()</code></strong>: an iterator over the components currently dirty on one archetype.</li><li><strong><code>world:dirtyArchetypes()</code></strong>: an iterator over the archetypes with at least one component dirty. The same rule as query iteration applies: do not mutate the world while iterating.</li></ul><div class="language-teal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">teal</span><pre class="shiki shiki-themes github-light-high-contrast tokyo-night vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">for</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> archetype </span><span style="--shiki-light:#A0111F;--shiki-dark:#89DDFF;">in</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> world:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">dirtyArchetypes</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">() </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">do</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    if</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;"> archetype:</span><span style="--shiki-light:#023B95;--shiki-dark:#0DB9D7;">isComponentDirty</span><span style="--shiki-light:#0E1116;--shiki-dark:#A9B1D6;">(Tint) </span><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">then</span></span>
<span class="line"><span style="--shiki-light:#66707B;--shiki-light-font-style:inherit;--shiki-dark:#51597D;--shiki-dark-font-style:italic;">        -- The Tint column was rewritten this frame.</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">    end</span></span>
<span class="line"><span style="--shiki-light:#A0111F;--shiki-dark:#BB9AF7;">end</span></span></code></pre></div><h2 id="explicit-marking" tabindex="-1">Explicit marking <a class="header-anchor" href="#explicit-marking" aria-label="Permalink to &quot;Explicit marking&quot;">​</a></h2><p>When a write happens through a path the framework can&#39;t see, call the marker directly:</p><ul><li><strong><code>archetype:markComponentDirty(Foo)</code></strong>: a single component. A no-op if the archetype doesn&#39;t carry it.</li><li><strong><code>archetype:markAllComponentsDirty()</code></strong>: every component on the archetype. This is what spawn, move-in and swap-pop use.</li><li><strong><code>world:markComponentDirty(entity, Foo)</code></strong>: the entity-shaped wrapper, for when you only have the id.</li></ul><p><code>archetype:set(row, value)</code> and the <code>world:set</code> family all mark internally; you only need the explicit form when you bypass them.</p><h2 id="lifecycle" tabindex="-1">Lifecycle <a class="header-anchor" href="#lifecycle" aria-label="Permalink to &quot;Lifecycle&quot;">​</a></h2><p>Bits are set at write time and cleared by <code>world:update</code> after the pipeline runs. The world keeps the set of archetypes that were marked, so the cost of clearing is proportional to the archetypes that actually changed, not to the total archetype count.</p><p>Newly spawned entities, and entities that move between archetypes, are automatically dirty for every component on the destination archetype. Removing an entity marks the source archetype too, because a swap-pop rewrites the bytes of the row it filled.</p><div class="warning custom-block"><p class="custom-block-title">batchSpawn writes rows itself</p><p><code>world:batchSpawn</code> claims a row range and hands it to your callback rather than running each component&#39;s constructor, so FFI <code>defaults</code> are not applied to those rows. Set every field in the callback; the placement itself has already marked the destination archetype&#39;s columns dirty.</p></div>`,30)])])}const y=t(n,[["render",o]]);export{k as __pageData,y as default};
